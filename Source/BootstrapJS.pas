unit BootstrapJS;

interface

uses Classes, Interfaces;

type
  TBootstrapJS = class(TInterfacedObject,iModelJS)
    private
      FPack : TStringList;
      FCDN: Boolean;
    public
      constructor Create;
      destructor Destroy; override;
      class function New : iModelJS;
	  procedure BootstrapJS_1;
      procedure BootstrapJS_2;
      procedure BootstrapJS_3;
      procedure BootstrapJS_4;
      procedure BootstrapJS_5;
      procedure BootstrapJS_6;
      procedure BootstrapJS_7;
      procedure BootstrapJS_8;
      procedure BootstrapJS_9;
      procedure BootstrapJS_10;
      procedure BootstrapJS_11;
      procedure BootstrapJS_12;
      function PackJS : String;
      function CDN(Value : Boolean) : iModelJS;
      function Credenciais(Value : iModelCredenciais) : iModelJS;
  end;

implementation

uses
  SysUtils;

{ TBootstrapJS }

function TBootstrapJS.CDN(Value: Boolean): iModelJS;
begin
  Result := Self;
  FCDN := Value;
end;

constructor TBootstrapJS.Create;
begin
  FPack := TStringList.Create;
end;

function TBootstrapJS.Credenciais(Value: iModelCredenciais): iModelJS;
begin
  Result := Self;
end;

destructor TBootstrapJS.Destroy;
begin
  freeandnil(fpack);
  inherited;
end;

class function TBootstrapJS.New: iModelJS;
begin
  Result := Self.Create;
end;

procedure TBootstrapJS.BootstrapJS_1;
begin
  FPack.Add('<script>'+#13);
  FPack.Add('/*!');
  FPack.Add('  * Bootstrap v5.3.3 (https://getbootstrap.com/)');
  FPack.Add('  * Copyright 2011-2024 The Bootstrap Authors (https://github.com/twbs/bootstrap/graphs/contributors)');
  FPack.Add('  * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('  */');
  FPack.Add('(function (global, factory) {');
  FPack.Add('  typeof exports === ''object'' && typeof module !== ''undefined'' ? module.exports = factory(require(''@popperjs/core'')) :');
  FPack.Add('  typeof define === ''function'' && define.amd ? define([''@popperjs/core''], factory) :');
  FPack.Add('  (global = typeof globalThis !== ''undefined'' ? globalThis : global || self, global.bootstrap = factory(global.Popper));');
  FPack.Add('})(this, (function (Popper) { ''use strict'';');
  FPack.Add('');
  FPack.Add('  function _interopNamespaceDefault(e) {');
  FPack.Add('    const n = Object.create(null, { [Symbol.toStringTag]: { value: ''Module'' } });');
  FPack.Add('    if (e) {');
  FPack.Add('      for (const k in e) {');
  FPack.Add('        if (k !== ''default'') {');
  FPack.Add('          const d = Object.getOwnPropertyDescriptor(e, k);');
  FPack.Add('          Object.defineProperty(n, k, d.get ? d : {');
  FPack.Add('            enumerable: true,');
  FPack.Add('            get: () => e[k]');
  FPack.Add('          });');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    n.default = e;');
  FPack.Add('    return Object.freeze(n);');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  const Popper__namespace = /*#__PURE__*/_interopNamespaceDefault(Popper);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap dom/data.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const elementMap = new Map();');
  FPack.Add('  const Data = {');
  FPack.Add('    set(element, key, instance) {');
  FPack.Add('      if (!elementMap.has(element)) {');
  FPack.Add('        elementMap.set(element, new Map());');
  FPack.Add('      }');
  FPack.Add('      const instanceMap = elementMap.get(element);');
  FPack.Add('');
  FPack.Add('      // make it clear we only want one instance per element');
  FPack.Add('      // can be removed later when multiple key/instances are fine to be used');
  FPack.Add('      if (!instanceMap.has(key) && instanceMap.size !== 0) {');
  FPack.Add('        // eslint-disable-next-line no-console');
  FPack.Add('        console.error(`Bootstrap doesn''t allow more than one instance per element. Bound instance: ${Array.from(instanceMap.keys())[0]}.`);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      instanceMap.set(key, instance);');
  FPack.Add('    },');
  FPack.Add('    get(element, key) {');
  FPack.Add('      if (elementMap.has(element)) {');
  FPack.Add('        return elementMap.get(element).get(key) || null;');
  FPack.Add('      }');
  FPack.Add('      return null;');
  FPack.Add('    },');
  FPack.Add('    remove(element, key) {');
  FPack.Add('      if (!elementMap.has(element)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const instanceMap = elementMap.get(element);');
  FPack.Add('      instanceMap.delete(key);');
  FPack.Add('');
  FPack.Add('      // free up element references if there are no instances left for an element');
  FPack.Add('      if (instanceMap.size === 0) {');
  FPack.Add('        elementMap.delete(element);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/index.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const MAX_UID = 1000000;');
  FPack.Add('  const MILLISECONDS_MULTIPLIER = 1000;');
  FPack.Add('  const TRANSITION_END = ''transitionend'';');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Properly escape IDs selectors to handle weird IDs');
  FPack.Add('   * @param {string} selector');
  FPack.Add('   * @returns {string}');
  FPack.Add('   */');
  FPack.Add('  const parseSelector = selector => {');
  FPack.Add('    if (selector && window.CSS && window.CSS.escape) {');
  FPack.Add('      // document.querySelector needs escaping to handle IDs (html5+) containing for instance /');
  FPack.Add('      selector = selector.replace(/#([^\s"#'']+)/g, (match, id) => `#${CSS.escape(id)}`);');
  FPack.Add('    }');
  FPack.Add('    return selector;');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  // Shout-out Angus Croll (https://goo.gl/pxwQGp)');
  FPack.Add('  const toType = object => {');
  FPack.Add('    if (object === null || object === undefined) {');
  FPack.Add('      return `${object}`;');
  FPack.Add('    }');
  FPack.Add('    return Object.prototype.toString.call(object).match(/\s([a-z]+)/i)[1].toLowerCase();');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Public Util API');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const getUID = prefix => {');
  FPack.Add('    do {');
  FPack.Add('      prefix += Math.floor(Math.random() * MAX_UID);');
  FPack.Add('    } while (document.getElementById(prefix));');
  FPack.Add('    return prefix;');
  FPack.Add('  };');
  FPack.Add('  const getTransitionDurationFromElement = element => {');
  FPack.Add('    if (!element) {');
  FPack.Add('      return 0;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Get transition-duration of the element');
  FPack.Add('    let {');
  FPack.Add('      transitionDuration,');
  FPack.Add('      transitionDelay');
  FPack.Add('    } = window.getComputedStyle(element);');
  FPack.Add('    const floatTransitionDuration = Number.parseFloat(transitionDuration);');
  FPack.Add('    const floatTransitionDelay = Number.parseFloat(transitionDelay);');
  FPack.Add('');
  FPack.Add('    // Return 0 if element or transition duration is not found');
  FPack.Add('    if (!floatTransitionDuration && !floatTransitionDelay) {');
  FPack.Add('      return 0;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // If multiple durations are defined, take the first');
  FPack.Add('    transitionDuration = transitionDuration.split('','')[0];');
  FPack.Add('    transitionDelay = transitionDelay.split('','')[0];');
  FPack.Add('    return (Number.parseFloat(transitionDuration) + Number.parseFloat(transitionDelay)) * MILLISECONDS_MULTIPLIER;');
  FPack.Add('  };');
  FPack.Add('  const triggerTransitionEnd = element => {');
  FPack.Add('    element.dispatchEvent(new Event(TRANSITION_END));');
  FPack.Add('  };');
  FPack.Add('  const isElement = object => {');
  FPack.Add('    if (!object || typeof object !== ''object'') {');
  FPack.Add('      return false;');
  FPack.Add('    }');
  FPack.Add('    if (typeof object.jquery !== ''undefined'') {');
  FPack.Add('      object = object[0];');
  FPack.Add('    }');
  FPack.Add('    return typeof object.nodeType !== ''undefined'';');
  FPack.Add('  };');
  FPack.Add('  const getElement = object => {');
  FPack.Add('    // it''s a jQuery object or a node element');
  FPack.Add('    if (isElement(object)) {');
  FPack.Add('      return object.jquery ? object[0] : object;');
  FPack.Add('    }');
  FPack.Add('    if (typeof object === ''string'' && object.length > 0) {');
  FPack.Add('      return document.querySelector(parseSelector(object));');
  FPack.Add('    }');
  FPack.Add('    return null;');
  FPack.Add('  };');
  FPack.Add('  const isVisible = element => {');
  FPack.Add('    if (!isElement(element) || element.getClientRects().length === 0) {');
  FPack.Add('      return false;');
  FPack.Add('    }');
  FPack.Add('    const elementIsVisible = getComputedStyle(element).getPropertyValue(''visibility'') === ''visible'';');
  FPack.Add('    // Handle `details` element as its content may falsie appear visible when it is closed');
  FPack.Add('    const closedDetails = element.closest(''details:not([open])'');');
  FPack.Add('    if (!closedDetails) {');
  FPack.Add('      return elementIsVisible;');
  FPack.Add('    }');
  FPack.Add('    if (closedDetails !== element) {');
  FPack.Add('      const summary = element.closest(''summary'');');
  FPack.Add('      if (summary && summary.parentNode !== closedDetails) {');
  FPack.Add('        return false;');
  FPack.Add('      }');
  FPack.Add('      if (summary === null) {');
  FPack.Add('        return false;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    return elementIsVisible;');
  FPack.Add('  };');
  FPack.Add('  const isDisabled = element => {');
  FPack.Add('    if (!element || element.nodeType !== Node.ELEMENT_NODE) {');
  FPack.Add('      return true;');
  FPack.Add('    }');
  FPack.Add('    if (element.classList.contains(''disabled'')) {');
  FPack.Add('      return true;');
  FPack.Add('    }');
  FPack.Add('    if (typeof element.disabled !== ''undefined'') {');
  FPack.Add('      return element.disabled;');
  FPack.Add('    }');
  FPack.Add('    return element.hasAttribute(''disabled'') && element.getAttribute(''disabled'') !== ''false'';');
  FPack.Add('  };');
  FPack.Add('  const findShadowRoot = element => {');
  FPack.Add('    if (!document.documentElement.attachShadow) {');
  FPack.Add('      return null;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Can find the shadow root otherwise it''ll return the document');
  FPack.Add('    if (typeof element.getRootNode === ''function'') {');
  FPack.Add('      const root = element.getRootNode();');
  FPack.Add('      return root instanceof ShadowRoot ? root : null;');
  FPack.Add('    }');
  FPack.Add('    if (element instanceof ShadowRoot) {');
  FPack.Add('      return element;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // when we don''t find a shadow root');
  FPack.Add('    if (!element.parentNode) {');
  FPack.Add('      return null;');
  FPack.Add('    }');
  FPack.Add('    return findShadowRoot(element.parentNode);');
  FPack.Add('  };');
  FPack.Add('  const noop = () => {};');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Trick to restart an element''s animation');
  FPack.Add('   *');
  FPack.Add('   * @param {HTMLElement} element');
  FPack.Add('   * @return void');
  FPack.Add('   *');
  FPack.Add('   * @see https://www.charistheo.io/blog/2021/02/restart-a-css-animation-with-javascript/#restarting-a-css-animation');
  FPack.Add('   */');
  FPack.Add('  const reflow = element => {');
  FPack.Add('    element.offsetHeight; // eslint-disable-line no-unused-expressions');
  FPack.Add('  };');
  FPack.Add('  const getjQuery = () => {');
  FPack.Add('    if (window.jQuery && !document.body.hasAttribute(''data-bs-no-jquery'')) {');
  FPack.Add('      return window.jQuery;');
  FPack.Add('    }');
  FPack.Add('    return null;');
  FPack.Add('  };');
  FPack.Add('  const DOMContentLoadedCallbacks = [];');
  FPack.Add('  const onDOMContentLoaded = callback => {');
  FPack.Add('    if (document.readyState === ''loading'') {');
  FPack.Add('      // add listener on the first call when the document is in loading state');
  FPack.Add('      if (!DOMContentLoadedCallbacks.length) {');
  FPack.Add('        document.addEventListener(''DOMContentLoaded'', () => {');
  FPack.Add('          for (const callback of DOMContentLoadedCallbacks) {');
  FPack.Add('            callback();');
  FPack.Add('          }');
  FPack.Add('        });');
  FPack.Add('      }');
  FPack.Add('      DOMContentLoadedCallbacks.push(callback);');
  FPack.Add('    } else {');
  FPack.Add('      callback();');
  FPack.Add('    }');
  FPack.Add('  };');
  FPack.Add('  const isRTL = () => document.documentElement.dir === ''rtl'';');
  FPack.Add('  const defineJQueryPlugin = plugin => {');
  FPack.Add('    onDOMContentLoaded(() => {');
  FPack.Add('      const $ = getjQuery();');
  FPack.Add('      /* istanbul ignore if */');
  FPack.Add('      if ($) {');
  FPack.Add('        const name = plugin.NAME;');
  FPack.Add('        const JQUERY_NO_CONFLICT = $.fn[name];');
  FPack.Add('        $.fn[name] = plugin.jQueryInterface;');
  FPack.Add('        $.fn[name].Constructor = plugin;');
  FPack.Add('        $.fn[name].noConflict = () => {');
  FPack.Add('          $.fn[name] = JQUERY_NO_CONFLICT;');
  FPack.Add('          return plugin.jQueryInterface;');
  FPack.Add('        };');
  FPack.Add('      }');
  FPack.Add('    });');
  FPack.Add('  };');
  FPack.Add('  const execute = (possibleCallback, args = [], defaultValue = possibleCallback) => {');
  FPack.Add('    return typeof possibleCallback === ''function'' ? possibleCallback(...args) : defaultValue;');
  FPack.Add('  };');
  FPack.Add('  const executeAfterTransition = (callback, transitionElement, waitForTransition = true) => {');
  FPack.Add('    if (!waitForTransition) {');
  FPack.Add('      execute(callback);');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    const durationPadding = 5;');
  FPack.Add('    const emulatedDuration = getTransitionDurationFromElement(transitionElement) + durationPadding;');
  FPack.Add('    let called = false;');
  FPack.Add('    const handler = ({');
  FPack.Add('      target');
  FPack.Add('    }) => {');
  FPack.Add('      if (target !== transitionElement) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      called = true;');
  FPack.Add('      transitionElement.removeEventListener(TRANSITION_END, handler);');
  FPack.Add('      execute(callback);');
  FPack.Add('    };');
  FPack.Add('    transitionElement.addEventListener(TRANSITION_END, handler);');
  FPack.Add('    setTimeout(() => {');
  FPack.Add('      if (!called) {');
  FPack.Add('        triggerTransitionEnd(transitionElement);');
  FPack.Add('      }');
  FPack.Add('    }, emulatedDuration);');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Return the previous/next element of a list.');
  FPack.Add('   *');
  FPack.Add('   * @param {array} list    The list of elements');
  FPack.Add('   * @param activeElement   The active element');
  FPack.Add('   * @param shouldGetNext   Choose to get next or previous element');
  FPack.Add('   * @param isCycleAllowed');
  FPack.Add('   * @return {Element|elem} The proper element');
  FPack.Add('   */');
  FPack.Add('  const getNextActiveElement = (list, activeElement, shouldGetNext, isCycleAllowed) => {');
  FPack.Add('    const listLength = list.length;');
  FPack.Add('    let index = list.indexOf(activeElement);');
  FPack.Add('');
  FPack.Add('    // if the element does not exist in the list return an element');
  FPack.Add('    // depending on the direction and if cycle is allowed');
  FPack.Add('    if (index === -1) {');
  FPack.Add('      return !shouldGetNext && isCycleAllowed ? list[listLength - 1] : list[0];');
  FPack.Add('    }');
  FPack.Add('    index += shouldGetNext ? 1 : -1;');
  FPack.Add('    if (isCycleAllowed) {');
  FPack.Add('      index = (index + listLength) % listLength;');
  FPack.Add('    }');
  FPack.Add('    return list[Math.max(0, Math.min(index, listLength - 1))];');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap dom/event-handler.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const namespaceRegex = /[^.]*(?=\..*)\.|.*/;');
  FPack.Add('  const stripNameRegex = /\..*/;');
  FPack.Add('  const stripUidRegex = /::\d+$/;');
  FPack.Add('  const eventRegistry = {}; // Events storage');
  FPack.Add('  let uidEvent = 1;');
  FPack.Add('  const customEvents = {');
  FPack.Add('    mouseenter: ''mouseover'',');
  FPack.Add('    mouseleave: ''mouseout''');
  FPack.Add('  };');
  FPack.Add('  const nativeEvents = new Set([''click'', ''dblclick'', ''mouseup'', ''mousedown'', ''contextmenu'', ''mousewheel'', ''DOMMouseScroll'', ''mouseover'', ''mouseout'', ''mousemove'', ''selectstart'', ''selectend'', ''keydown'', ''keypress'', ''keyup'''+', ''orientationchange'', ''touchstart'', ''touchmove'', ''touchend'', ''touchcancel'', ''pointerdown'', ''pointermove'', ''pointerup'', ''pointerleave'', ''pointercancel'', ''gesturestart'', ''gesturechange'', ''gestureend'', ''focus'', ''blur'', ''c'+'hange'', ''reset'', ''select'', ''submit'', ''focusin'', ''focusout'', ''load'', ''unload'', ''beforeunload'', ''resize'', ''move'', ''DOMContentLoaded'', ''readystatechange'', ''error'', ''abort'', ''scroll'']);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Private methods');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  function makeEventUid(element, uid) {');
  FPack.Add('    return uid && `${uid}::${uidEvent++}` || element.uidEvent || uidEvent++;');
  FPack.Add('  }');
  FPack.Add('  function getElementEvents(element) {');
  FPack.Add('    const uid = makeEventUid(element);');
  FPack.Add('    element.uidEvent = uid;');
  FPack.Add('    eventRegistry[uid] = eventRegistry[uid] || {};');
  FPack.Add('    return eventRegistry[uid];');
  FPack.Add('  }');
  FPack.Add('  function bootstrapHandler(element, fn) {');
  FPack.Add('    return function handler(event) {');
  FPack.Add('      hydrateObj(event, {');
  FPack.Add('        delegateTarget: element');
  FPack.Add('      });');
  FPack.Add('      if (handler.oneOff) {');
  FPack.Add('        EventHandler.off(element, event.type, fn);');
  FPack.Add('      }');
  FPack.Add('      return fn.apply(element, [event]);');
  FPack.Add('    };');
  FPack.Add('  }');
  FPack.Add('  function bootstrapDelegationHandler(element, selector, fn) {');
  FPack.Add('    return function handler(event) {');
  FPack.Add('      const domElements = element.querySelectorAll(selector);');
  FPack.Add('      for (let {');
  FPack.Add('        target');
  FPack.Add('      } = event; target && target !== this; target = target.parentNode) {');
  FPack.Add('        for (const domElement of domElements) {');
  FPack.Add('          if (domElement !== target) {');
  FPack.Add('            continue;');
  FPack.Add('          }');
  FPack.Add('          hydrateObj(event, {');
  FPack.Add('            delegateTarget: target');
  FPack.Add('          });');
  FPack.Add('          if (handler.oneOff) {');
  FPack.Add('            EventHandler.off(element, event.type, selector, fn);');
  FPack.Add('          }');
  FPack.Add('          return fn.apply(target, [event]);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    };');
  FPack.Add('  }');
  FPack.Add('  function findHandler(events, callable, delegationSelector = null) {');
  FPack.Add('    return Object.values(events).find(event => event.callable === callable && event.delegationSelector === delegationSelector);');
  FPack.Add('  }');
  FPack.Add('  function normalizeParameters(originalTypeEvent, handler, delegationFunction) {');
  FPack.Add('    const isDelegated = typeof handler === ''string'';');
  FPack.Add('    // TODO: tooltip passes `false` instead of selector, so we need to check');
  FPack.Add('    const callable = isDelegated ? delegationFunction : handler || delegationFunction;');
end;

procedure TBootstrapJS.BootstrapJS_2;
begin
  FPack.Add('    let typeEvent = getTypeEvent(originalTypeEvent);');
  FPack.Add('    if (!nativeEvents.has(typeEvent)) {');
  FPack.Add('      typeEvent = originalTypeEvent;');
  FPack.Add('    }');
  FPack.Add('    return [isDelegated, callable, typeEvent];');
  FPack.Add('  }');
  FPack.Add('  function addHandler(element, originalTypeEvent, handler, delegationFunction, oneOff) {');
  FPack.Add('    if (typeof originalTypeEvent !== ''string'' || !element) {');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    let [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationFunction);');
  FPack.Add('');
  FPack.Add('    // in case of mouseenter or mouseleave wrap the handler within a function that checks for its DOM position');
  FPack.Add('    // this prevents the handler from being dispatched the same way as mouseover or mouseout does');
  FPack.Add('    if (originalTypeEvent in customEvents) {');
  FPack.Add('      const wrapFunction = fn => {');
  FPack.Add('        return function (event) {');
  FPack.Add('          if (!event.relatedTarget || event.relatedTarget !== event.delegateTarget && !event.delegateTarget.contains(event.relatedTarget)) {');
  FPack.Add('            return fn.call(this, event);');
  FPack.Add('          }');
  FPack.Add('        };');
  FPack.Add('      };');
  FPack.Add('      callable = wrapFunction(callable);');
  FPack.Add('    }');
  FPack.Add('    const events = getElementEvents(element);');
  FPack.Add('    const handlers = events[typeEvent] || (events[typeEvent] = {});');
  FPack.Add('    const previousFunction = findHandler(handlers, callable, isDelegated ? handler : null);');
  FPack.Add('    if (previousFunction) {');
  FPack.Add('      previousFunction.oneOff = previousFunction.oneOff && oneOff;');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    const uid = makeEventUid(callable, originalTypeEvent.replace(namespaceRegex, ''''));');
  FPack.Add('    const fn = isDelegated ? bootstrapDelegationHandler(element, handler, callable) : bootstrapHandler(element, callable);');
  FPack.Add('    fn.delegationSelector = isDelegated ? handler : null;');
  FPack.Add('    fn.callable = callable;');
  FPack.Add('    fn.oneOff = oneOff;');
  FPack.Add('    fn.uidEvent = uid;');
  FPack.Add('    handlers[uid] = fn;');
  FPack.Add('    element.addEventListener(typeEvent, fn, isDelegated);');
  FPack.Add('  }');
  FPack.Add('  function removeHandler(element, events, typeEvent, handler, delegationSelector) {');
  FPack.Add('    const fn = findHandler(events[typeEvent], handler, delegationSelector);');
  FPack.Add('    if (!fn) {');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    element.removeEventListener(typeEvent, fn, Boolean(delegationSelector));');
  FPack.Add('    delete events[typeEvent][fn.uidEvent];');
  FPack.Add('  }');
  FPack.Add('  function removeNamespacedHandlers(element, events, typeEvent, namespace) {');
  FPack.Add('    const storeElementEvent = events[typeEvent] || {};');
  FPack.Add('    for (const [handlerKey, event] of Object.entries(storeElementEvent)) {');
  FPack.Add('      if (handlerKey.includes(namespace)) {');
  FPack.Add('        removeHandler(element, events, typeEvent, event.callable, event.delegationSelector);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('  function getTypeEvent(event) {');
  FPack.Add('    // allow to get the native events from namespaced events (''click.bs.button'' --> ''click'')');
  FPack.Add('    event = event.replace(stripNameRegex, '''');');
  FPack.Add('    return customEvents[event] || event;');
  FPack.Add('  }');
  FPack.Add('  const EventHandler = {');
  FPack.Add('    on(element, event, handler, delegationFunction) {');
  FPack.Add('      addHandler(element, event, handler, delegationFunction, false);');
  FPack.Add('    },');
  FPack.Add('    one(element, event, handler, delegationFunction) {');
  FPack.Add('      addHandler(element, event, handler, delegationFunction, true);');
  FPack.Add('    },');
  FPack.Add('    off(element, originalTypeEvent, handler, delegationFunction) {');
  FPack.Add('      if (typeof originalTypeEvent !== ''string'' || !element) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationFunction);');
  FPack.Add('      const inNamespace = typeEvent !== originalTypeEvent;');
  FPack.Add('      const events = getElementEvents(element);');
  FPack.Add('      const storeElementEvent = events[typeEvent] || {};');
  FPack.Add('      const isNamespace = originalTypeEvent.startsWith(''.'');');
  FPack.Add('      if (typeof callable !== ''undefined'') {');
  FPack.Add('        // Simplest case: handler is passed, remove that listener ONLY.');
  FPack.Add('        if (!Object.keys(storeElementEvent).length) {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        removeHandler(element, events, typeEvent, callable, isDelegated ? handler : null);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (isNamespace) {');
  FPack.Add('        for (const elementEvent of Object.keys(events)) {');
  FPack.Add('          removeNamespacedHandlers(element, events, elementEvent, originalTypeEvent.slice(1));');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      for (const [keyHandlers, event] of Object.entries(storeElementEvent)) {');
  FPack.Add('        const handlerKey = keyHandlers.replace(stripUidRegex, '''');');
  FPack.Add('        if (!inNamespace || originalTypeEvent.includes(handlerKey)) {');
  FPack.Add('          removeHandler(element, events, typeEvent, event.callable, event.delegationSelector);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    },');
  FPack.Add('    trigger(element, event, args) {');
  FPack.Add('      if (typeof event !== ''string'' || !element) {');
  FPack.Add('        return null;');
  FPack.Add('      }');
  FPack.Add('      const $ = getjQuery();');
  FPack.Add('      const typeEvent = getTypeEvent(event);');
  FPack.Add('      const inNamespace = event !== typeEvent;');
  FPack.Add('      let jQueryEvent = null;');
  FPack.Add('      let bubbles = true;');
  FPack.Add('      let nativeDispatch = true;');
  FPack.Add('      let defaultPrevented = false;');
  FPack.Add('      if (inNamespace && $) {');
  FPack.Add('        jQueryEvent = $.Event(event, args);');
  FPack.Add('        $(element).trigger(jQueryEvent);');
  FPack.Add('        bubbles = !jQueryEvent.isPropagationStopped();');
  FPack.Add('        nativeDispatch = !jQueryEvent.isImmediatePropagationStopped();');
  FPack.Add('        defaultPrevented = jQueryEvent.isDefaultPrevented();');
  FPack.Add('      }');
  FPack.Add('      const evt = hydrateObj(new Event(event, {');
  FPack.Add('        bubbles,');
  FPack.Add('        cancelable: true');
  FPack.Add('      }), args);');
  FPack.Add('      if (defaultPrevented) {');
  FPack.Add('        evt.preventDefault();');
  FPack.Add('      }');
  FPack.Add('      if (nativeDispatch) {');
  FPack.Add('        element.dispatchEvent(evt);');
  FPack.Add('      }');
  FPack.Add('      if (evt.defaultPrevented && jQueryEvent) {');
  FPack.Add('        jQueryEvent.preventDefault();');
  FPack.Add('      }');
  FPack.Add('      return evt;');
  FPack.Add('    }');
  FPack.Add('  };');
  FPack.Add('  function hydrateObj(obj, meta = {}) {');
  FPack.Add('    for (const [key, value] of Object.entries(meta)) {');
  FPack.Add('      try {');
  FPack.Add('        obj[key] = value;');
  FPack.Add('      } catch (_unused) {');
  FPack.Add('        Object.defineProperty(obj, key, {');
  FPack.Add('          configurable: true,');
  FPack.Add('          get() {');
  FPack.Add('            return value;');
  FPack.Add('          }');
  FPack.Add('        });');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    return obj;');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap dom/manipulator.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  function normalizeData(value) {');
  FPack.Add('    if (value === ''true'') {');
  FPack.Add('      return true;');
  FPack.Add('    }');
  FPack.Add('    if (value === ''false'') {');
  FPack.Add('      return false;');
  FPack.Add('    }');
  FPack.Add('    if (value === Number(value).toString()) {');
  FPack.Add('      return Number(value);');
  FPack.Add('    }');
  FPack.Add('    if (value === '''' || value === ''null'') {');
  FPack.Add('      return null;');
  FPack.Add('    }');
  FPack.Add('    if (typeof value !== ''string'') {');
  FPack.Add('      return value;');
  FPack.Add('    }');
  FPack.Add('    try {');
  FPack.Add('      return JSON.parse(decodeURIComponent(value));');
  FPack.Add('    } catch (_unused) {');
  FPack.Add('      return value;');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('  function normalizeDataKey(key) {');
  FPack.Add('    return key.replace(/[A-Z]/g, chr => `-${chr.toLowerCase()}`);');
  FPack.Add('  }');
  FPack.Add('  const Manipulator = {');
  FPack.Add('    setDataAttribute(element, key, value) {');
  FPack.Add('      element.setAttribute(`data-bs-${normalizeDataKey(key)}`, value);');
  FPack.Add('    },');
  FPack.Add('    removeDataAttribute(element, key) {');
  FPack.Add('      element.removeAttribute(`data-bs-${normalizeDataKey(key)}`);');
  FPack.Add('    },');
  FPack.Add('    getDataAttributes(element) {');
  FPack.Add('      if (!element) {');
  FPack.Add('        return {};');
  FPack.Add('      }');
  FPack.Add('      const attributes = {};');
  FPack.Add('      const bsKeys = Object.keys(element.dataset).filter(key => key.startsWith(''bs'') && !key.startsWith(''bsConfig''));');
  FPack.Add('      for (const key of bsKeys) {');
  FPack.Add('        let pureKey = key.replace(/^bs/, '''');');
  FPack.Add('        pureKey = pureKey.charAt(0).toLowerCase() + pureKey.slice(1, pureKey.length);');
  FPack.Add('        attributes[pureKey] = normalizeData(element.dataset[key]);');
  FPack.Add('      }');
  FPack.Add('      return attributes;');
  FPack.Add('    },');
  FPack.Add('    getDataAttribute(element, key) {');
  FPack.Add('      return normalizeData(element.getAttribute(`data-bs-${normalizeDataKey(key)}`));');
  FPack.Add('    }');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/config.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Config {');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return {};');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return {};');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      throw new Error(''You have to implement the static method "NAME", for each component!'');');
  FPack.Add('    }');
  FPack.Add('    _getConfig(config) {');
  FPack.Add('      config = this._mergeConfigObj(config);');
  FPack.Add('      config = this._configAfterMerge(config);');
  FPack.Add('      this._typeCheckConfig(config);');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _mergeConfigObj(config, element) {');
  FPack.Add('      const jsonConfig = isElement(element) ? Manipulator.getDataAttribute(element, ''config'') : {}; // try to parse');
  FPack.Add('');
  FPack.Add('      return {');
  FPack.Add('        ...this.constructor.Default,');
  FPack.Add('        ...(typeof jsonConfig === ''object'' ? jsonConfig : {}),');
  FPack.Add('        ...(isElement(element) ? Manipulator.getDataAttributes(element) : {}),');
  FPack.Add('        ...(typeof config === ''object'' ? config : {})');
  FPack.Add('      };');
  FPack.Add('    }');
  FPack.Add('    _typeCheckConfig(config, configTypes = this.constructor.DefaultType) {');
  FPack.Add('      for (const [property, expectedTypes] of Object.entries(configTypes)) {');
  FPack.Add('        const value = config[property];');
  FPack.Add('        const valueType = isElement(value) ? ''element'' : toType(value);');
  FPack.Add('        if (!new RegExp(expectedTypes).test(valueType)) {');
  FPack.Add('          throw new TypeError(`${this.constructor.NAME.toUpperCase()}: Option "${property}" provided type "${valueType}" but expected type "${expectedTypes}".`);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap base-component.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const VERSION = ''5.3.3'';');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class BaseComponent extends Config {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super();');
  FPack.Add('      element = getElement(element);');
  FPack.Add('      if (!element) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._element = element;');
  FPack.Add('      this._config = this._getConfig(config);');
  FPack.Add('      Data.set(this._element, this.constructor.DATA_KEY, this);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    dispose() {');
  FPack.Add('      Data.remove(this._element, this.constructor.DATA_KEY);');
  FPack.Add('      EventHandler.off(this._element, this.constructor.EVENT_KEY);');
  FPack.Add('      for (const propertyName of Object.getOwnPropertyNames(this)) {');
  FPack.Add('        this[propertyName] = null;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _queueCallback(callback, element, isAnimated = true) {');
  FPack.Add('      executeAfterTransition(callback, element, isAnimated);');
  FPack.Add('    }');
  FPack.Add('    _getConfig(config) {');
  FPack.Add('      config = this._mergeConfigObj(config, this._element);');
  FPack.Add('      config = this._configAfterMerge(config);');
  FPack.Add('      this._typeCheckConfig(config);');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static getInstance(element) {');
  FPack.Add('      return Data.get(getElement(element), this.DATA_KEY);');
  FPack.Add('    }');
  FPack.Add('    static getOrCreateInstance(element, config = {}) {');
  FPack.Add('      return this.getInstance(element) || new this(element, typeof config === ''object'' ? config : null);');
  FPack.Add('    }');
  FPack.Add('    static get VERSION() {');
  FPack.Add('      return VERSION;');
  FPack.Add('    }');
  FPack.Add('    static get DATA_KEY() {');
  FPack.Add('      return `bs.${this.NAME}`;');
  FPack.Add('    }');
  FPack.Add('    static get EVENT_KEY() {');
  FPack.Add('      return `.${this.DATA_KEY}`;');
  FPack.Add('    }');
  FPack.Add('    static eventName(name) {');
  FPack.Add('      return `${name}${this.EVENT_KEY}`;');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap dom/selector-engine.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const getSelector = element => {');
  FPack.Add('    let selector = element.getAttribute(''data-bs-target'');');
  FPack.Add('    if (!selector || selector === ''#'') {');
  FPack.Add('      let hrefAttribute = element.getAttribute(''href'');');
  FPack.Add('');
  FPack.Add('      // The only valid content that could double as a selector are IDs or classes,');
  FPack.Add('      // so everything starting with `#` or `.`. If a "real" URL is used as the selector,');
  FPack.Add('      // `document.querySelector` will rightfully complain it is invalid.');
  FPack.Add('      // See https://github.com/twbs/bootstrap/issues/32273');
  FPack.Add('      if (!hrefAttribute || !hrefAttribute.includes(''#'') && !hrefAttribute.startsWith(''.'')) {');
  FPack.Add('        return null;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // Just in case some CMS puts out a full URL with the anchor appended');
  FPack.Add('      if (hrefAttribute.includes(''#'') && !hrefAttribute.startsWith(''#'')) {');
  FPack.Add('        hrefAttribute = `#${hrefAttribute.split(''#'')[1]}`;');
  FPack.Add('      }');
  FPack.Add('      selector = hrefAttribute && hrefAttribute !== ''#'' ? hrefAttribute.trim() : null;');
  FPack.Add('    }');
  FPack.Add('    return selector ? selector.split('','').map(sel => parseSelector(sel)).join('','') : null;');
  FPack.Add('  };');
  FPack.Add('  const SelectorEngine = {');
  FPack.Add('    find(selector, element = document.documentElement) {');
  FPack.Add('      return [].concat(...Element.prototype.querySelectorAll.call(element, selector));');
  FPack.Add('    },');
  FPack.Add('    findOne(selector, element = document.documentElement) {');
  FPack.Add('      return Element.prototype.querySelector.call(element, selector);');
  FPack.Add('    },');
  FPack.Add('    children(element, selector) {');
  FPack.Add('      return [].concat(...element.children).filter(child => child.matches(selector));');
  FPack.Add('    },');
  FPack.Add('    parents(element, selector) {');
  FPack.Add('      const parents = [];');
  FPack.Add('      let ancestor = element.parentNode.closest(selector);');
  FPack.Add('      while (ancestor) {');
  FPack.Add('        parents.push(ancestor);');
  FPack.Add('        ancestor = ancestor.parentNode.closest(selector);');
  FPack.Add('      }');
  FPack.Add('      return parents;');
  FPack.Add('    },');
  FPack.Add('    prev(element, selector) {');
  FPack.Add('      let previous = element.previousElementSibling;');
  FPack.Add('      while (previous) {');
  FPack.Add('        if (previous.matches(selector)) {');
  FPack.Add('          return [previous];');
  FPack.Add('        }');
  FPack.Add('        previous = previous.previousElementSibling;');
  FPack.Add('      }');
  FPack.Add('      return [];');
  FPack.Add('    },');
  FPack.Add('    // TODO: this is now unused; remove later along with prev()');
  FPack.Add('    next(element, selector) {');
  FPack.Add('      let next = element.nextElementSibling;');
  FPack.Add('      while (next) {');
  FPack.Add('        if (next.matches(selector)) {');
  FPack.Add('          return [next];');
  FPack.Add('        }');
  FPack.Add('        next = next.nextElementSibling;');
  FPack.Add('      }');
  FPack.Add('      return [];');
  FPack.Add('    },');
  FPack.Add('    focusableChildren(element) {');
  FPack.Add('      const focusables = [''a'', ''button'', ''input'', ''textarea'', ''select'', ''details'', ''[tabindex]'', ''[contenteditable="true"]''].map(selector => `${selector}:not([tabindex^="-"])`).join('','');');
  FPack.Add('      return this.find(focusables, element).filter(el => !isDisabled(el) && isVisible(el));');
  FPack.Add('    },');
  FPack.Add('    getSelectorFromElement(element) {');
  FPack.Add('      const selector = getSelector(element);');
end;

procedure TBootstrapJS.BootstrapJS_3;
begin
  FPack.Add('      if (selector) {');
  FPack.Add('        return SelectorEngine.findOne(selector) ? selector : null;');
  FPack.Add('      }');
  FPack.Add('      return null;');
  FPack.Add('    },');
  FPack.Add('    getElementFromSelector(element) {');
  FPack.Add('      const selector = getSelector(element);');
  FPack.Add('      return selector ? SelectorEngine.findOne(selector) : null;');
  FPack.Add('    },');
  FPack.Add('    getMultipleElementsFromSelector(element) {');
  FPack.Add('      const selector = getSelector(element);');
  FPack.Add('      return selector ? SelectorEngine.find(selector) : [];');
  FPack.Add('    }');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/component-functions.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const enableDismissTrigger = (component, method = ''hide'') => {');
  FPack.Add('    const clickEvent = `click.dismiss${component.EVENT_KEY}`;');
  FPack.Add('    const name = component.NAME;');
  FPack.Add('    EventHandler.on(document, clickEvent, `[data-bs-dismiss="${name}"]`, function (event) {');
  FPack.Add('      if ([''A'', ''AREA''].includes(this.tagName)) {');
  FPack.Add('        event.preventDefault();');
  FPack.Add('      }');
  FPack.Add('      if (isDisabled(this)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const target = SelectorEngine.getElementFromSelector(this) || this.closest(`.${name}`);');
  FPack.Add('      const instance = component.getOrCreateInstance(target);');
  FPack.Add('');
  FPack.Add('      // Method argument is left, for Alert and only, as it doesn''t implement the ''hide'' method');
  FPack.Add('      instance[method]();');
  FPack.Add('    });');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap alert.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$f = ''alert'';');
  FPack.Add('  const DATA_KEY$a = ''bs.alert'';');
  FPack.Add('  const EVENT_KEY$b = `.${DATA_KEY$a}`;');
  FPack.Add('  const EVENT_CLOSE = `close${EVENT_KEY$b}`;');
  FPack.Add('  const EVENT_CLOSED = `closed${EVENT_KEY$b}`;');
  FPack.Add('  const CLASS_NAME_FADE$5 = ''fade'';');
  FPack.Add('  const CLASS_NAME_SHOW$8 = ''show'';');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Alert extends BaseComponent {');
  FPack.Add('    // Getters');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$f;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    close() {');
  FPack.Add('      const closeEvent = EventHandler.trigger(this._element, EVENT_CLOSE);');
  FPack.Add('      if (closeEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_SHOW$8);');
  FPack.Add('      const isAnimated = this._element.classList.contains(CLASS_NAME_FADE$5);');
  FPack.Add('      this._queueCallback(() => this._destroyElement(), this._element, isAnimated);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _destroyElement() {');
  FPack.Add('      this._element.remove();');
  FPack.Add('      EventHandler.trigger(this._element, EVENT_CLOSED);');
  FPack.Add('      this.dispose();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Alert.getOrCreateInstance(this);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (data[config] === undefined || config.startsWith(''_'') || config === ''constructor'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config](this);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  enableDismissTrigger(Alert, ''close'');');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Alert);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap button.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$e = ''button'';');
  FPack.Add('  const DATA_KEY$9 = ''bs.button'';');
  FPack.Add('  const EVENT_KEY$a = `.${DATA_KEY$9}`;');
  FPack.Add('  const DATA_API_KEY$6 = ''.data-api'';');
  FPack.Add('  const CLASS_NAME_ACTIVE$3 = ''active'';');
  FPack.Add('  const SELECTOR_DATA_TOGGLE$5 = ''[data-bs-toggle="button"]'';');
  FPack.Add('  const EVENT_CLICK_DATA_API$6 = `click${EVENT_KEY$a}${DATA_API_KEY$6}`;');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Button extends BaseComponent {');
  FPack.Add('    // Getters');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$e;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    toggle() {');
  FPack.Add('      // Toggle class and sync the `aria-pressed` attribute with the return value of the `.toggle()` method');
  FPack.Add('      this._element.setAttribute(''aria-pressed'', this._element.classList.toggle(CLASS_NAME_ACTIVE$3));');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Button.getOrCreateInstance(this);');
  FPack.Add('        if (config === ''toggle'') {');
  FPack.Add('          data[config]();');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$6, SELECTOR_DATA_TOGGLE$5, event => {');
  FPack.Add('    event.preventDefault();');
  FPack.Add('    const button = event.target.closest(SELECTOR_DATA_TOGGLE$5);');
  FPack.Add('    const data = Button.getOrCreateInstance(button);');
  FPack.Add('    data.toggle();');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Button);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/swipe.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$d = ''swipe'';');
  FPack.Add('  const EVENT_KEY$9 = ''.bs.swipe'';');
  FPack.Add('  const EVENT_TOUCHSTART = `touchstart${EVENT_KEY$9}`;');
  FPack.Add('  const EVENT_TOUCHMOVE = `touchmove${EVENT_KEY$9}`;');
  FPack.Add('  const EVENT_TOUCHEND = `touchend${EVENT_KEY$9}`;');
  FPack.Add('  const EVENT_POINTERDOWN = `pointerdown${EVENT_KEY$9}`;');
  FPack.Add('  const EVENT_POINTERUP = `pointerup${EVENT_KEY$9}`;');
  FPack.Add('  const POINTER_TYPE_TOUCH = ''touch'';');
  FPack.Add('  const POINTER_TYPE_PEN = ''pen'';');
  FPack.Add('  const CLASS_NAME_POINTER_EVENT = ''pointer-event'';');
  FPack.Add('  const SWIPE_THRESHOLD = 40;');
  FPack.Add('  const Default$c = {');
  FPack.Add('    endCallback: null,');
  FPack.Add('    leftCallback: null,');
  FPack.Add('    rightCallback: null');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$c = {');
  FPack.Add('    endCallback: ''(function|null)'',');
  FPack.Add('    leftCallback: ''(function|null)'',');
  FPack.Add('    rightCallback: ''(function|null)''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Swipe extends Config {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super();');
  FPack.Add('      this._element = element;');
  FPack.Add('      if (!element || !Swipe.isSupported()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._config = this._getConfig(config);');
  FPack.Add('      this._deltaX = 0;');
  FPack.Add('      this._supportPointerEvents = Boolean(window.PointerEvent);');
  FPack.Add('      this._initEvents();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$c;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$c;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$d;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    dispose() {');
  FPack.Add('      EventHandler.off(this._element, EVENT_KEY$9);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _start(event) {');
  FPack.Add('      if (!this._supportPointerEvents) {');
  FPack.Add('        this._deltaX = event.touches[0].clientX;');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (this._eventIsPointerPenTouch(event)) {');
  FPack.Add('        this._deltaX = event.clientX;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _end(event) {');
  FPack.Add('      if (this._eventIsPointerPenTouch(event)) {');
  FPack.Add('        this._deltaX = event.clientX - this._deltaX;');
  FPack.Add('      }');
  FPack.Add('      this._handleSwipe();');
  FPack.Add('      execute(this._config.endCallback);');
  FPack.Add('    }');
  FPack.Add('    _move(event) {');
  FPack.Add('      this._deltaX = event.touches && event.touches.length > 1 ? 0 : event.touches[0].clientX - this._deltaX;');
  FPack.Add('    }');
  FPack.Add('    _handleSwipe() {');
  FPack.Add('      const absDeltaX = Math.abs(this._deltaX);');
  FPack.Add('      if (absDeltaX <= SWIPE_THRESHOLD) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const direction = absDeltaX / this._deltaX;');
  FPack.Add('      this._deltaX = 0;');
  FPack.Add('      if (!direction) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      execute(direction > 0 ? this._config.rightCallback : this._config.leftCallback);');
  FPack.Add('    }');
  FPack.Add('    _initEvents() {');
  FPack.Add('      if (this._supportPointerEvents) {');
  FPack.Add('        EventHandler.on(this._element, EVENT_POINTERDOWN, event => this._start(event));');
  FPack.Add('        EventHandler.on(this._element, EVENT_POINTERUP, event => this._end(event));');
  FPack.Add('        this._element.classList.add(CLASS_NAME_POINTER_EVENT);');
  FPack.Add('      } else {');
  FPack.Add('        EventHandler.on(this._element, EVENT_TOUCHSTART, event => this._start(event));');
  FPack.Add('        EventHandler.on(this._element, EVENT_TOUCHMOVE, event => this._move(event));');
  FPack.Add('        EventHandler.on(this._element, EVENT_TOUCHEND, event => this._end(event));');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _eventIsPointerPenTouch(event) {');
  FPack.Add('      return this._supportPointerEvents && (event.pointerType === POINTER_TYPE_PEN || event.pointerType === POINTER_TYPE_TOUCH);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static isSupported() {');
  FPack.Add('      return ''ontouchstart'' in document.documentElement || navigator.maxTouchPoints > 0;');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap carousel.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$c = ''carousel'';');
  FPack.Add('  const DATA_KEY$8 = ''bs.carousel'';');
  FPack.Add('  const EVENT_KEY$8 = `.${DATA_KEY$8}`;');
  FPack.Add('  const DATA_API_KEY$5 = ''.data-api'';');
  FPack.Add('  const ARROW_LEFT_KEY$1 = ''ArrowLeft'';');
  FPack.Add('  const ARROW_RIGHT_KEY$1 = ''ArrowRight'';');
  FPack.Add('  const TOUCHEVENT_COMPAT_WAIT = 500; // Time for mouse compat events to fire after touch');
  FPack.Add('');
  FPack.Add('  const ORDER_NEXT = ''next'';');
  FPack.Add('  const ORDER_PREV = ''prev'';');
  FPack.Add('  const DIRECTION_LEFT = ''left'';');
  FPack.Add('  const DIRECTION_RIGHT = ''right'';');
  FPack.Add('  const EVENT_SLIDE = `slide${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_SLID = `slid${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_KEYDOWN$1 = `keydown${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_MOUSEENTER$1 = `mouseenter${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_MOUSELEAVE$1 = `mouseleave${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_DRAG_START = `dragstart${EVENT_KEY$8}`;');
  FPack.Add('  const EVENT_LOAD_DATA_API$3 = `load${EVENT_KEY$8}${DATA_API_KEY$5}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API$5 = `click${EVENT_KEY$8}${DATA_API_KEY$5}`;');
  FPack.Add('  const CLASS_NAME_CAROUSEL = ''carousel'';');
  FPack.Add('  const CLASS_NAME_ACTIVE$2 = ''active'';');
  FPack.Add('  const CLASS_NAME_SLIDE = ''slide'';');
  FPack.Add('  const CLASS_NAME_END = ''carousel-item-end'';');
  FPack.Add('  const CLASS_NAME_START = ''carousel-item-start'';');
  FPack.Add('  const CLASS_NAME_NEXT = ''carousel-item-next'';');
  FPack.Add('  const CLASS_NAME_PREV = ''carousel-item-prev'';');
  FPack.Add('  const SELECTOR_ACTIVE = ''.active'';');
  FPack.Add('  const SELECTOR_ITEM = ''.carousel-item'';');
  FPack.Add('  const SELECTOR_ACTIVE_ITEM = SELECTOR_ACTIVE + SELECTOR_ITEM;');
  FPack.Add('  const SELECTOR_ITEM_IMG = ''.carousel-item img'';');
  FPack.Add('  const SELECTOR_INDICATORS = ''.carousel-indicators'';');
  FPack.Add('  const SELECTOR_DATA_SLIDE = ''[data-bs-slide], [data-bs-slide-to]'';');
  FPack.Add('  const SELECTOR_DATA_RIDE = ''[data-bs-ride="carousel"]'';');
  FPack.Add('  const KEY_TO_DIRECTION = {');
  FPack.Add('    [ARROW_LEFT_KEY$1]: DIRECTION_RIGHT,');
  FPack.Add('    [ARROW_RIGHT_KEY$1]: DIRECTION_LEFT');
  FPack.Add('  };');
  FPack.Add('  const Default$b = {');
  FPack.Add('    interval: 5000,');
  FPack.Add('    keyboard: true,');
  FPack.Add('    pause: ''hover'',');
  FPack.Add('    ride: false,');
  FPack.Add('    touch: true,');
  FPack.Add('    wrap: true');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$b = {');
  FPack.Add('    interval: ''(number|boolean)'',');
  FPack.Add('    // TODO:v6 remove boolean support');
  FPack.Add('    keyboard: ''boolean'',');
  FPack.Add('    pause: ''(string|boolean)'',');
  FPack.Add('    ride: ''(boolean|string)'',');
  FPack.Add('    touch: ''boolean'',');
  FPack.Add('    wrap: ''boolean''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Carousel extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._interval = null;');
  FPack.Add('      this._activeElement = null;');
  FPack.Add('      this._isSliding = false;');
  FPack.Add('      this.touchTimeout = null;');
  FPack.Add('      this._swipeHelper = null;');
  FPack.Add('      this._indicatorsElement = SelectorEngine.findOne(SELECTOR_INDICATORS, this._element);');
  FPack.Add('      this._addEventListeners();');
  FPack.Add('      if (this._config.ride === CLASS_NAME_CAROUSEL) {');
  FPack.Add('        this.cycle();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$b;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$b;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$c;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    next() {');
  FPack.Add('      this._slide(ORDER_NEXT);');
end;

procedure TBootstrapJS.BootstrapJS_4;
begin
  FPack.Add('    }');
  FPack.Add('    nextWhenVisible() {');
  FPack.Add('      // FIXME TODO use `document.visibilityState`');
  FPack.Add('      // Don''t call next when the page isn''t visible');
  FPack.Add('      // or the carousel or its parent isn''t visible');
  FPack.Add('      if (!document.hidden && isVisible(this._element)) {');
  FPack.Add('        this.next();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    prev() {');
  FPack.Add('      this._slide(ORDER_PREV);');
  FPack.Add('    }');
  FPack.Add('    pause() {');
  FPack.Add('      if (this._isSliding) {');
  FPack.Add('        triggerTransitionEnd(this._element);');
  FPack.Add('      }');
  FPack.Add('      this._clearInterval();');
  FPack.Add('    }');
  FPack.Add('    cycle() {');
  FPack.Add('      this._clearInterval();');
  FPack.Add('      this._updateInterval();');
  FPack.Add('      this._interval = setInterval(() => this.nextWhenVisible(), this._config.interval);');
  FPack.Add('    }');
  FPack.Add('    _maybeEnableCycle() {');
  FPack.Add('      if (!this._config.ride) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (this._isSliding) {');
  FPack.Add('        EventHandler.one(this._element, EVENT_SLID, () => this.cycle());');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this.cycle();');
  FPack.Add('    }');
  FPack.Add('    to(index) {');
  FPack.Add('      const items = this._getItems();');
  FPack.Add('      if (index > items.length - 1 || index < 0) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (this._isSliding) {');
  FPack.Add('        EventHandler.one(this._element, EVENT_SLID, () => this.to(index));');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const activeIndex = this._getItemIndex(this._getActive());');
  FPack.Add('      if (activeIndex === index) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const order = index > activeIndex ? ORDER_NEXT : ORDER_PREV;');
  FPack.Add('      this._slide(order, items[index]);');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      if (this._swipeHelper) {');
  FPack.Add('        this._swipeHelper.dispose();');
  FPack.Add('      }');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      config.defaultInterval = config.interval;');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _addEventListeners() {');
  FPack.Add('      if (this._config.keyboard) {');
  FPack.Add('        EventHandler.on(this._element, EVENT_KEYDOWN$1, event => this._keydown(event));');
  FPack.Add('      }');
  FPack.Add('      if (this._config.pause === ''hover'') {');
  FPack.Add('        EventHandler.on(this._element, EVENT_MOUSEENTER$1, () => this.pause());');
  FPack.Add('        EventHandler.on(this._element, EVENT_MOUSELEAVE$1, () => this._maybeEnableCycle());');
  FPack.Add('      }');
  FPack.Add('      if (this._config.touch && Swipe.isSupported()) {');
  FPack.Add('        this._addTouchEventListeners();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _addTouchEventListeners() {');
  FPack.Add('      for (const img of SelectorEngine.find(SELECTOR_ITEM_IMG, this._element)) {');
  FPack.Add('        EventHandler.on(img, EVENT_DRAG_START, event => event.preventDefault());');
  FPack.Add('      }');
  FPack.Add('      const endCallBack = () => {');
  FPack.Add('        if (this._config.pause !== ''hover'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('');
  FPack.Add('        // If it''s a touch-enabled device, mouseenter/leave are fired as');
  FPack.Add('        // part of the mouse compatibility events on first tap - the carousel');
  FPack.Add('        // would stop cycling until user tapped out of it;');
  FPack.Add('        // here, we listen for touchend, explicitly pause the carousel');
  FPack.Add('        // (as if it''s the second time we tap on it, mouseenter compat event');
  FPack.Add('        // is NOT fired) and after a timeout (to allow for mouse compatibility');
  FPack.Add('        // events to fire) we explicitly restart cycling');
  FPack.Add('');
  FPack.Add('        this.pause();');
  FPack.Add('        if (this.touchTimeout) {');
  FPack.Add('          clearTimeout(this.touchTimeout);');
  FPack.Add('        }');
  FPack.Add('        this.touchTimeout = setTimeout(() => this._maybeEnableCycle(), TOUCHEVENT_COMPAT_WAIT + this._config.interval);');
  FPack.Add('      };');
  FPack.Add('      const swipeConfig = {');
  FPack.Add('        leftCallback: () => this._slide(this._directionToOrder(DIRECTION_LEFT)),');
  FPack.Add('        rightCallback: () => this._slide(this._directionToOrder(DIRECTION_RIGHT)),');
  FPack.Add('        endCallback: endCallBack');
  FPack.Add('      };');
  FPack.Add('      this._swipeHelper = new Swipe(this._element, swipeConfig);');
  FPack.Add('    }');
  FPack.Add('    _keydown(event) {');
  FPack.Add('      if (/input|textarea/i.test(event.target.tagName)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const direction = KEY_TO_DIRECTION[event.key];');
  FPack.Add('      if (direction) {');
  FPack.Add('        event.preventDefault();');
  FPack.Add('        this._slide(this._directionToOrder(direction));');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _getItemIndex(element) {');
  FPack.Add('      return this._getItems().indexOf(element);');
  FPack.Add('    }');
  FPack.Add('    _setActiveIndicatorElement(index) {');
  FPack.Add('      if (!this._indicatorsElement) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const activeIndicator = SelectorEngine.findOne(SELECTOR_ACTIVE, this._indicatorsElement);');
  FPack.Add('      activeIndicator.classList.remove(CLASS_NAME_ACTIVE$2);');
  FPack.Add('      activeIndicator.removeAttribute(''aria-current'');');
  FPack.Add('      const newActiveIndicator = SelectorEngine.findOne(`[data-bs-slide-to="${index}"]`, this._indicatorsElement);');
  FPack.Add('      if (newActiveIndicator) {');
  FPack.Add('        newActiveIndicator.classList.add(CLASS_NAME_ACTIVE$2);');
  FPack.Add('        newActiveIndicator.setAttribute(''aria-current'', ''true'');');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _updateInterval() {');
  FPack.Add('      const element = this._activeElement || this._getActive();');
  FPack.Add('      if (!element) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const elementInterval = Number.parseInt(element.getAttribute(''data-bs-interval''), 10);');
  FPack.Add('      this._config.interval = elementInterval || this._config.defaultInterval;');
  FPack.Add('    }');
  FPack.Add('    _slide(order, element = null) {');
  FPack.Add('      if (this._isSliding) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const activeElement = this._getActive();');
  FPack.Add('      const isNext = order === ORDER_NEXT;');
  FPack.Add('      const nextElement = element || getNextActiveElement(this._getItems(), activeElement, isNext, this._config.wrap);');
  FPack.Add('      if (nextElement === activeElement) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const nextElementIndex = this._getItemIndex(nextElement);');
  FPack.Add('      const triggerEvent = eventName => {');
  FPack.Add('        return EventHandler.trigger(this._element, eventName, {');
  FPack.Add('          relatedTarget: nextElement,');
  FPack.Add('          direction: this._orderToDirection(order),');
  FPack.Add('          from: this._getItemIndex(activeElement),');
  FPack.Add('          to: nextElementIndex');
  FPack.Add('        });');
  FPack.Add('      };');
  FPack.Add('      const slideEvent = triggerEvent(EVENT_SLIDE);');
  FPack.Add('      if (slideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (!activeElement || !nextElement) {');
  FPack.Add('        // Some weirdness is happening, so we bail');
  FPack.Add('        // TODO: change tests that use empty divs to avoid this check');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const isCycling = Boolean(this._interval);');
  FPack.Add('      this.pause();');
  FPack.Add('      this._isSliding = true;');
  FPack.Add('      this._setActiveIndicatorElement(nextElementIndex);');
  FPack.Add('      this._activeElement = nextElement;');
  FPack.Add('      const directionalClassName = isNext ? CLASS_NAME_START : CLASS_NAME_END;');
  FPack.Add('      const orderClassName = isNext ? CLASS_NAME_NEXT : CLASS_NAME_PREV;');
  FPack.Add('      nextElement.classList.add(orderClassName);');
  FPack.Add('      reflow(nextElement);');
  FPack.Add('      activeElement.classList.add(directionalClassName);');
  FPack.Add('      nextElement.classList.add(directionalClassName);');
  FPack.Add('      const completeCallBack = () => {');
  FPack.Add('        nextElement.classList.remove(directionalClassName, orderClassName);');
  FPack.Add('        nextElement.classList.add(CLASS_NAME_ACTIVE$2);');
  FPack.Add('        activeElement.classList.remove(CLASS_NAME_ACTIVE$2, orderClassName, directionalClassName);');
  FPack.Add('        this._isSliding = false;');
  FPack.Add('        triggerEvent(EVENT_SLID);');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(completeCallBack, activeElement, this._isAnimated());');
  FPack.Add('      if (isCycling) {');
  FPack.Add('        this.cycle();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _isAnimated() {');
  FPack.Add('      return this._element.classList.contains(CLASS_NAME_SLIDE);');
  FPack.Add('    }');
  FPack.Add('    _getActive() {');
  FPack.Add('      return SelectorEngine.findOne(SELECTOR_ACTIVE_ITEM, this._element);');
  FPack.Add('    }');
  FPack.Add('    _getItems() {');
  FPack.Add('      return SelectorEngine.find(SELECTOR_ITEM, this._element);');
  FPack.Add('    }');
  FPack.Add('    _clearInterval() {');
  FPack.Add('      if (this._interval) {');
  FPack.Add('        clearInterval(this._interval);');
  FPack.Add('        this._interval = null;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _directionToOrder(direction) {');
  FPack.Add('      if (isRTL()) {');
  FPack.Add('        return direction === DIRECTION_LEFT ? ORDER_PREV : ORDER_NEXT;');
  FPack.Add('      }');
  FPack.Add('      return direction === DIRECTION_LEFT ? ORDER_NEXT : ORDER_PREV;');
  FPack.Add('    }');
  FPack.Add('    _orderToDirection(order) {');
  FPack.Add('      if (isRTL()) {');
  FPack.Add('        return order === ORDER_PREV ? DIRECTION_LEFT : DIRECTION_RIGHT;');
  FPack.Add('      }');
  FPack.Add('      return order === ORDER_PREV ? DIRECTION_RIGHT : DIRECTION_LEFT;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Carousel.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config === ''number'') {');
  FPack.Add('          data.to(config);');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (typeof config === ''string'') {');
  FPack.Add('          if (data[config] === undefined || config.startsWith(''_'') || config === ''constructor'') {');
  FPack.Add('            throw new TypeError(`No method named "${config}"`);');
  FPack.Add('          }');
  FPack.Add('          data[config]();');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$5, SELECTOR_DATA_SLIDE, function (event) {');
  FPack.Add('    const target = SelectorEngine.getElementFromSelector(this);');
  FPack.Add('    if (!target || !target.classList.contains(CLASS_NAME_CAROUSEL)) {');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    event.preventDefault();');
  FPack.Add('    const carousel = Carousel.getOrCreateInstance(target);');
  FPack.Add('    const slideIndex = this.getAttribute(''data-bs-slide-to'');');
  FPack.Add('    if (slideIndex) {');
  FPack.Add('      carousel.to(slideIndex);');
  FPack.Add('      carousel._maybeEnableCycle();');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    if (Manipulator.getDataAttribute(this, ''slide'') === ''next'') {');
  FPack.Add('      carousel.next();');
  FPack.Add('      carousel._maybeEnableCycle();');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    carousel.prev();');
  FPack.Add('    carousel._maybeEnableCycle();');
  FPack.Add('  });');
  FPack.Add('  EventHandler.on(window, EVENT_LOAD_DATA_API$3, () => {');
  FPack.Add('    const carousels = SelectorEngine.find(SELECTOR_DATA_RIDE);');
  FPack.Add('    for (const carousel of carousels) {');
  FPack.Add('      Carousel.getOrCreateInstance(carousel);');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Carousel);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap collapse.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$b = ''collapse'';');
  FPack.Add('  const DATA_KEY$7 = ''bs.collapse'';');
  FPack.Add('  const EVENT_KEY$7 = `.${DATA_KEY$7}`;');
  FPack.Add('  const DATA_API_KEY$4 = ''.data-api'';');
  FPack.Add('  const EVENT_SHOW$6 = `show${EVENT_KEY$7}`;');
  FPack.Add('  const EVENT_SHOWN$6 = `shown${EVENT_KEY$7}`;');
  FPack.Add('  const EVENT_HIDE$6 = `hide${EVENT_KEY$7}`;');
  FPack.Add('  const EVENT_HIDDEN$6 = `hidden${EVENT_KEY$7}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API$4 = `click${EVENT_KEY$7}${DATA_API_KEY$4}`;');
  FPack.Add('  const CLASS_NAME_SHOW$7 = ''show'';');
  FPack.Add('  const CLASS_NAME_COLLAPSE = ''collapse'';');
  FPack.Add('  const CLASS_NAME_COLLAPSING = ''collapsing'';');
  FPack.Add('  const CLASS_NAME_COLLAPSED = ''collapsed'';');
  FPack.Add('  const CLASS_NAME_DEEPER_CHILDREN = `:scope .${CLASS_NAME_COLLAPSE} .${CLASS_NAME_COLLAPSE}`;');
  FPack.Add('  const CLASS_NAME_HORIZONTAL = ''collapse-horizontal'';');
  FPack.Add('  const WIDTH = ''width'';');
  FPack.Add('  const HEIGHT = ''height'';');
  FPack.Add('  const SELECTOR_ACTIVES = ''.collapse.show, .collapse.collapsing'';');
  FPack.Add('  const SELECTOR_DATA_TOGGLE$4 = ''[data-bs-toggle="collapse"]'';');
  FPack.Add('  const Default$a = {');
  FPack.Add('    parent: null,');
  FPack.Add('    toggle: true');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$a = {');
  FPack.Add('    parent: ''(null|element)'',');
  FPack.Add('    toggle: ''boolean''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Collapse extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._isTransitioning = false;');
  FPack.Add('      this._triggerArray = [];');
  FPack.Add('      const toggleList = SelectorEngine.find(SELECTOR_DATA_TOGGLE$4);');
  FPack.Add('      for (const elem of toggleList) {');
  FPack.Add('        const selector = SelectorEngine.getSelectorFromElement(elem);');
  FPack.Add('        const filterElement = SelectorEngine.find(selector).filter(foundElement => foundElement === this._element);');
  FPack.Add('        if (selector !== null && filterElement.length) {');
  FPack.Add('          this._triggerArray.push(elem);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      this._initializeChildren();');
  FPack.Add('      if (!this._config.parent) {');
  FPack.Add('        this._addAriaAndCollapsedClass(this._triggerArray, this._isShown());');
  FPack.Add('      }');
  FPack.Add('      if (this._config.toggle) {');
  FPack.Add('        this.toggle();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$a;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$a;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$b;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    toggle() {');
  FPack.Add('      if (this._isShown()) {');
  FPack.Add('        this.hide();');
  FPack.Add('      } else {');
  FPack.Add('        this.show();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    show() {');
  FPack.Add('      if (this._isTransitioning || this._isShown()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      let activeChildren = [];');
  FPack.Add('');
  FPack.Add('      // find active children');
  FPack.Add('      if (this._config.parent) {');
  FPack.Add('        activeChildren = this._getFirstLevelChildren(SELECTOR_ACTIVES).filter(element => element !== this._element).map(element => Collapse.getOrCreateInstance(element, {');
  FPack.Add('          toggle: false');
  FPack.Add('        }));');
  FPack.Add('      }');
  FPack.Add('      if (activeChildren.length && activeChildren[0]._isTransitioning) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const startEvent = EventHandler.trigger(this._element, EVENT_SHOW$6);');
  FPack.Add('      if (startEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      for (const activeInstance of activeChildren) {');
  FPack.Add('        activeInstance.hide();');
  FPack.Add('      }');
  FPack.Add('      const dimension = this._getDimension();');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_COLLAPSE);');
  FPack.Add('      this._element.classList.add(CLASS_NAME_COLLAPSING);');
  FPack.Add('      this._element.style[dimension] = 0;');
  FPack.Add('      this._addAriaAndCollapsedClass(this._triggerArray, true);');
  FPack.Add('      this._isTransitioning = true;');
  FPack.Add('      const complete = () => {');
  FPack.Add('        this._isTransitioning = false;');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_COLLAPSING);');
  FPack.Add('        this._element.classList.add(CLASS_NAME_COLLAPSE, CLASS_NAME_SHOW$7);');
  FPack.Add('        this._element.style[dimension] = '''';');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_SHOWN$6);');
  FPack.Add('      };');
  FPack.Add('      const capitalizedDimension = dimension[0].toUpperCase() + dimension.slice(1);');
  FPack.Add('      const scrollSize = `scroll${capitalizedDimension}`;');
  FPack.Add('      this._queueCallback(complete, this._element, true);');
  FPack.Add('      this._element.style[dimension] = `${this._element[scrollSize]}px`;');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      if (this._isTransitioning || !this._isShown()) {');
  FPack.Add('        return;');
end;

procedure TBootstrapJS.BootstrapJS_5;
begin
  FPack.Add('      }');
  FPack.Add('      const startEvent = EventHandler.trigger(this._element, EVENT_HIDE$6);');
  FPack.Add('      if (startEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const dimension = this._getDimension();');
  FPack.Add('      this._element.style[dimension] = `${this._element.getBoundingClientRect()[dimension]}px`;');
  FPack.Add('      reflow(this._element);');
  FPack.Add('      this._element.classList.add(CLASS_NAME_COLLAPSING);');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_COLLAPSE, CLASS_NAME_SHOW$7);');
  FPack.Add('      for (const trigger of this._triggerArray) {');
  FPack.Add('        const element = SelectorEngine.getElementFromSelector(trigger);');
  FPack.Add('        if (element && !this._isShown(element)) {');
  FPack.Add('          this._addAriaAndCollapsedClass([trigger], false);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      this._isTransitioning = true;');
  FPack.Add('      const complete = () => {');
  FPack.Add('        this._isTransitioning = false;');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_COLLAPSING);');
  FPack.Add('        this._element.classList.add(CLASS_NAME_COLLAPSE);');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_HIDDEN$6);');
  FPack.Add('      };');
  FPack.Add('      this._element.style[dimension] = '''';');
  FPack.Add('      this._queueCallback(complete, this._element, true);');
  FPack.Add('    }');
  FPack.Add('    _isShown(element = this._element) {');
  FPack.Add('      return element.classList.contains(CLASS_NAME_SHOW$7);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      config.toggle = Boolean(config.toggle); // Coerce string values');
  FPack.Add('      config.parent = getElement(config.parent);');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _getDimension() {');
  FPack.Add('      return this._element.classList.contains(CLASS_NAME_HORIZONTAL) ? WIDTH : HEIGHT;');
  FPack.Add('    }');
  FPack.Add('    _initializeChildren() {');
  FPack.Add('      if (!this._config.parent) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const children = this._getFirstLevelChildren(SELECTOR_DATA_TOGGLE$4);');
  FPack.Add('      for (const element of children) {');
  FPack.Add('        const selected = SelectorEngine.getElementFromSelector(element);');
  FPack.Add('        if (selected) {');
  FPack.Add('          this._addAriaAndCollapsedClass([element], this._isShown(selected));');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _getFirstLevelChildren(selector) {');
  FPack.Add('      const children = SelectorEngine.find(CLASS_NAME_DEEPER_CHILDREN, this._config.parent);');
  FPack.Add('      // remove children if greater depth');
  FPack.Add('      return SelectorEngine.find(selector, this._config.parent).filter(element => !children.includes(element));');
  FPack.Add('    }');
  FPack.Add('    _addAriaAndCollapsedClass(triggerArray, isOpen) {');
  FPack.Add('      if (!triggerArray.length) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      for (const element of triggerArray) {');
  FPack.Add('        element.classList.toggle(CLASS_NAME_COLLAPSED, !isOpen);');
  FPack.Add('        element.setAttribute(''aria-expanded'', isOpen);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      const _config = {};');
  FPack.Add('      if (typeof config === ''string'' && /show|hide/.test(config)) {');
  FPack.Add('        _config.toggle = false;');
  FPack.Add('      }');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Collapse.getOrCreateInstance(this, _config);');
  FPack.Add('        if (typeof config === ''string'') {');
  FPack.Add('          if (typeof data[config] === ''undefined'') {');
  FPack.Add('            throw new TypeError(`No method named "${config}"`);');
  FPack.Add('          }');
  FPack.Add('          data[config]();');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$4, SELECTOR_DATA_TOGGLE$4, function (event) {');
  FPack.Add('    // preventDefault only for <a> elements (which change the URL) not inside the collapsible element');
  FPack.Add('    if (event.target.tagName === ''A'' || event.delegateTarget && event.delegateTarget.tagName === ''A'') {');
  FPack.Add('      event.preventDefault();');
  FPack.Add('    }');
  FPack.Add('    for (const element of SelectorEngine.getMultipleElementsFromSelector(this)) {');
  FPack.Add('      Collapse.getOrCreateInstance(element, {');
  FPack.Add('        toggle: false');
  FPack.Add('      }).toggle();');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Collapse);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap dropdown.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$a = ''dropdown'';');
  FPack.Add('  const DATA_KEY$6 = ''bs.dropdown'';');
  FPack.Add('  const EVENT_KEY$6 = `.${DATA_KEY$6}`;');
  FPack.Add('  const DATA_API_KEY$3 = ''.data-api'';');
  FPack.Add('  const ESCAPE_KEY$2 = ''Escape'';');
  FPack.Add('  const TAB_KEY$1 = ''Tab'';');
  FPack.Add('  const ARROW_UP_KEY$1 = ''ArrowUp'';');
  FPack.Add('  const ARROW_DOWN_KEY$1 = ''ArrowDown'';');
  FPack.Add('  const RIGHT_MOUSE_BUTTON = 2; // MouseEvent.button value for the secondary button, usually the right button');
  FPack.Add('');
  FPack.Add('  const EVENT_HIDE$5 = `hide${EVENT_KEY$6}`;');
  FPack.Add('  const EVENT_HIDDEN$5 = `hidden${EVENT_KEY$6}`;');
  FPack.Add('  const EVENT_SHOW$5 = `show${EVENT_KEY$6}`;');
  FPack.Add('  const EVENT_SHOWN$5 = `shown${EVENT_KEY$6}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API$3 = `click${EVENT_KEY$6}${DATA_API_KEY$3}`;');
  FPack.Add('  const EVENT_KEYDOWN_DATA_API = `keydown${EVENT_KEY$6}${DATA_API_KEY$3}`;');
  FPack.Add('  const EVENT_KEYUP_DATA_API = `keyup${EVENT_KEY$6}${DATA_API_KEY$3}`;');
  FPack.Add('  const CLASS_NAME_SHOW$6 = ''show'';');
  FPack.Add('  const CLASS_NAME_DROPUP = ''dropup'';');
  FPack.Add('  const CLASS_NAME_DROPEND = ''dropend'';');
  FPack.Add('  const CLASS_NAME_DROPSTART = ''dropstart'';');
  FPack.Add('  const CLASS_NAME_DROPUP_CENTER = ''dropup-center'';');
  FPack.Add('  const CLASS_NAME_DROPDOWN_CENTER = ''dropdown-center'';');
  FPack.Add('  const SELECTOR_DATA_TOGGLE$3 = ''[data-bs-toggle="dropdown"]:not(.disabled):not(:disabled)'';');
  FPack.Add('  const SELECTOR_DATA_TOGGLE_SHOWN = `${SELECTOR_DATA_TOGGLE$3}.${CLASS_NAME_SHOW$6}`;');
  FPack.Add('  const SELECTOR_MENU = ''.dropdown-menu'';');
  FPack.Add('  const SELECTOR_NAVBAR = ''.navbar'';');
  FPack.Add('  const SELECTOR_NAVBAR_NAV = ''.navbar-nav'';');
  FPack.Add('  const SELECTOR_VISIBLE_ITEMS = ''.dropdown-menu .dropdown-item:not(.disabled):not(:disabled)'';');
  FPack.Add('  const PLACEMENT_TOP = isRTL() ? ''top-end'' : ''top-start'';');
  FPack.Add('  const PLACEMENT_TOPEND = isRTL() ? ''top-start'' : ''top-end'';');
  FPack.Add('  const PLACEMENT_BOTTOM = isRTL() ? ''bottom-end'' : ''bottom-start'';');
  FPack.Add('  const PLACEMENT_BOTTOMEND = isRTL() ? ''bottom-start'' : ''bottom-end'';');
  FPack.Add('  const PLACEMENT_RIGHT = isRTL() ? ''left-start'' : ''right-start'';');
  FPack.Add('  const PLACEMENT_LEFT = isRTL() ? ''right-start'' : ''left-start'';');
  FPack.Add('  const PLACEMENT_TOPCENTER = ''top'';');
  FPack.Add('  const PLACEMENT_BOTTOMCENTER = ''bottom'';');
  FPack.Add('  const Default$9 = {');
  FPack.Add('    autoClose: true,');
  FPack.Add('    boundary: ''clippingParents'',');
  FPack.Add('    display: ''dynamic'',');
  FPack.Add('    offset: [0, 2],');
  FPack.Add('    popperConfig: null,');
  FPack.Add('    reference: ''toggle''');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$9 = {');
  FPack.Add('    autoClose: ''(boolean|string)'',');
  FPack.Add('    boundary: ''(string|element)'',');
  FPack.Add('    display: ''string'',');
  FPack.Add('    offset: ''(array|string|function)'',');
  FPack.Add('    popperConfig: ''(null|object|function)'',');
  FPack.Add('    reference: ''(string|element|object)''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Dropdown extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._popper = null;');
  FPack.Add('      this._parent = this._element.parentNode; // dropdown wrapper');
  FPack.Add('      // TODO: v6 revert #37011 & change markup https://getbootstrap.com/docs/5.3/forms/input-group/');
  FPack.Add('      this._menu = SelectorEngine.next(this._element, SELECTOR_MENU)[0] || SelectorEngine.prev(this._element, SELECTOR_MENU)[0] || SelectorEngine.findOne(SELECTOR_MENU, this._parent);');
  FPack.Add('      this._inNavbar = this._detectNavbar();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$9;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$9;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$a;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    toggle() {');
  FPack.Add('      return this._isShown() ? this.hide() : this.show();');
  FPack.Add('    }');
  FPack.Add('    show() {');
  FPack.Add('      if (isDisabled(this._element) || this._isShown()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const relatedTarget = {');
  FPack.Add('        relatedTarget: this._element');
  FPack.Add('      };');
  FPack.Add('      const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$5, relatedTarget);');
  FPack.Add('      if (showEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._createPopper();');
  FPack.Add('');
  FPack.Add('      // If this is a touch-enabled device we add extra');
  FPack.Add('      // empty mouseover listeners to the body''s immediate children;');
  FPack.Add('      // only needed because of broken event delegation on iOS');
  FPack.Add('      // https://www.quirksmode.org/blog/archives/2014/02/mouse_event_bub.html');
  FPack.Add('      if (''ontouchstart'' in document.documentElement && !this._parent.closest(SELECTOR_NAVBAR_NAV)) {');
  FPack.Add('        for (const element of [].concat(...document.body.children)) {');
  FPack.Add('          EventHandler.on(element, ''mouseover'', noop);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      this._element.focus();');
  FPack.Add('      this._element.setAttribute(''aria-expanded'', true);');
  FPack.Add('      this._menu.classList.add(CLASS_NAME_SHOW$6);');
  FPack.Add('      this._element.classList.add(CLASS_NAME_SHOW$6);');
  FPack.Add('      EventHandler.trigger(this._element, EVENT_SHOWN$5, relatedTarget);');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      if (isDisabled(this._element) || !this._isShown()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const relatedTarget = {');
  FPack.Add('        relatedTarget: this._element');
  FPack.Add('      };');
  FPack.Add('      this._completeHide(relatedTarget);');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      if (this._popper) {');
  FPack.Add('        this._popper.destroy();');
  FPack.Add('      }');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('    update() {');
  FPack.Add('      this._inNavbar = this._detectNavbar();');
  FPack.Add('      if (this._popper) {');
  FPack.Add('        this._popper.update();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _completeHide(relatedTarget) {');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$5, relatedTarget);');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // If this is a touch-enabled device we remove the extra');
  FPack.Add('      // empty mouseover listeners we added for iOS support');
  FPack.Add('      if (''ontouchstart'' in document.documentElement) {');
  FPack.Add('        for (const element of [].concat(...document.body.children)) {');
  FPack.Add('          EventHandler.off(element, ''mouseover'', noop);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      if (this._popper) {');
  FPack.Add('        this._popper.destroy();');
  FPack.Add('      }');
  FPack.Add('      this._menu.classList.remove(CLASS_NAME_SHOW$6);');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_SHOW$6);');
  FPack.Add('      this._element.setAttribute(''aria-expanded'', ''false'');');
  FPack.Add('      Manipulator.removeDataAttribute(this._menu, ''popper'');');
  FPack.Add('      EventHandler.trigger(this._element, EVENT_HIDDEN$5, relatedTarget);');
  FPack.Add('    }');
  FPack.Add('    _getConfig(config) {');
  FPack.Add('      config = super._getConfig(config);');
  FPack.Add('      if (typeof config.reference === ''object'' && !isElement(config.reference) && typeof config.reference.getBoundingClientRect !== ''function'') {');
  FPack.Add('        // Popper virtual elements require a getBoundingClientRect method');
  FPack.Add('        throw new TypeError(`${NAME$a.toUpperCase()}: Option "reference" provided type "object" without a required "getBoundingClientRect" method.`);');
  FPack.Add('      }');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _createPopper() {');
  FPack.Add('      if (typeof Popper__namespace === ''undefined'') {');
  FPack.Add('        throw new TypeError(''Bootstrap\''s dropdowns require Popper (https://popper.js.org)'');');
  FPack.Add('      }');
  FPack.Add('      let referenceElement = this._element;');
  FPack.Add('      if (this._config.reference === ''parent'') {');
  FPack.Add('        referenceElement = this._parent;');
  FPack.Add('      } else if (isElement(this._config.reference)) {');
  FPack.Add('        referenceElement = getElement(this._config.reference);');
  FPack.Add('      } else if (typeof this._config.reference === ''object'') {');
  FPack.Add('        referenceElement = this._config.reference;');
  FPack.Add('      }');
  FPack.Add('      const popperConfig = this._getPopperConfig();');
  FPack.Add('      this._popper = Popper__namespace.createPopper(referenceElement, this._menu, popperConfig);');
  FPack.Add('    }');
  FPack.Add('    _isShown() {');
  FPack.Add('      return this._menu.classList.contains(CLASS_NAME_SHOW$6);');
  FPack.Add('    }');
  FPack.Add('    _getPlacement() {');
  FPack.Add('      const parentDropdown = this._parent;');
  FPack.Add('      if (parentDropdown.classList.contains(CLASS_NAME_DROPEND)) {');
  FPack.Add('        return PLACEMENT_RIGHT;');
  FPack.Add('      }');
  FPack.Add('      if (parentDropdown.classList.contains(CLASS_NAME_DROPSTART)) {');
  FPack.Add('        return PLACEMENT_LEFT;');
  FPack.Add('      }');
  FPack.Add('      if (parentDropdown.classList.contains(CLASS_NAME_DROPUP_CENTER)) {');
  FPack.Add('        return PLACEMENT_TOPCENTER;');
  FPack.Add('      }');
  FPack.Add('      if (parentDropdown.classList.contains(CLASS_NAME_DROPDOWN_CENTER)) {');
  FPack.Add('        return PLACEMENT_BOTTOMCENTER;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // We need to trim the value because custom properties can also include spaces');
  FPack.Add('      const isEnd = getComputedStyle(this._menu).getPropertyValue(''--bs-position'').trim() === ''end'';');
  FPack.Add('      if (parentDropdown.classList.contains(CLASS_NAME_DROPUP)) {');
  FPack.Add('        return isEnd ? PLACEMENT_TOPEND : PLACEMENT_TOP;');
  FPack.Add('      }');
  FPack.Add('      return isEnd ? PLACEMENT_BOTTOMEND : PLACEMENT_BOTTOM;');
  FPack.Add('    }');
  FPack.Add('    _detectNavbar() {');
  FPack.Add('      return this._element.closest(SELECTOR_NAVBAR) !== null;');
  FPack.Add('    }');
  FPack.Add('    _getOffset() {');
  FPack.Add('      const {');
  FPack.Add('        offset');
  FPack.Add('      } = this._config;');
  FPack.Add('      if (typeof offset === ''string'') {');
  FPack.Add('        return offset.split('','').map(value => Number.parseInt(value, 10));');
  FPack.Add('      }');
  FPack.Add('      if (typeof offset === ''function'') {');
  FPack.Add('        return popperData => offset(popperData, this._element);');
  FPack.Add('      }');
  FPack.Add('      return offset;');
  FPack.Add('    }');
  FPack.Add('    _getPopperConfig() {');
  FPack.Add('      const defaultBsPopperConfig = {');
  FPack.Add('        placement: this._getPlacement(),');
  FPack.Add('        modifiers: [{');
  FPack.Add('          name: ''preventOverflow'',');
  FPack.Add('          options: {');
  FPack.Add('            boundary: this._config.boundary');
  FPack.Add('          }');
  FPack.Add('        }, {');
  FPack.Add('          name: ''offset'',');
  FPack.Add('          options: {');
  FPack.Add('            offset: this._getOffset()');
  FPack.Add('          }');
  FPack.Add('        }]');
  FPack.Add('      };');
  FPack.Add('');
  FPack.Add('      // Disable Popper if we have a static display or Dropdown is in Navbar');
  FPack.Add('      if (this._inNavbar || this._config.display === ''static'') {');
  FPack.Add('        Manipulator.setDataAttribute(this._menu, ''popper'', ''static''); // TODO: v6 remove');
  FPack.Add('        defaultBsPopperConfig.modifiers = [{');
  FPack.Add('          name: ''applyStyles'',');
  FPack.Add('          enabled: false');
  FPack.Add('        }];');
  FPack.Add('      }');
  FPack.Add('      return {');
  FPack.Add('        ...defaultBsPopperConfig,');
  FPack.Add('        ...execute(this._config.popperConfig, [defaultBsPopperConfig])');
  FPack.Add('      };');
  FPack.Add('    }');
  FPack.Add('    _selectMenuItem({');
  FPack.Add('      key,');
  FPack.Add('      target');
  FPack.Add('    }) {');
  FPack.Add('      const items = SelectorEngine.find(SELECTOR_VISIBLE_ITEMS, this._menu).filter(element => isVisible(element));');
  FPack.Add('      if (!items.length) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // if target isn''t included in items (e.g. when expanding the dropdown)');
  FPack.Add('      // allow cycling to get the last item in case key equals ARROW_UP_KEY');
  FPack.Add('      getNextActiveElement(items, target, key === ARROW_DOWN_KEY$1, !items.includes(target)).focus();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Dropdown.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (typeof data[config] === ''undefined'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config]();');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    static clearMenus(event) {');
  FPack.Add('      if (event.button === RIGHT_MOUSE_BUTTON || event.type === ''keyup'' && event.key !== TAB_KEY$1) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const openToggles = SelectorEngine.find(SELECTOR_DATA_TOGGLE_SHOWN);');
  FPack.Add('      for (const toggle of openToggles) {');
  FPack.Add('        const context = Dropdown.getInstance(toggle);');
end;

procedure TBootstrapJS.BootstrapJS_6;
begin
  FPack.Add('        if (!context || context._config.autoClose === false) {');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('        const composedPath = event.composedPath();');
  FPack.Add('        const isMenuTarget = composedPath.includes(context._menu);');
  FPack.Add('        if (composedPath.includes(context._element) || context._config.autoClose === ''inside'' && !isMenuTarget || context._config.autoClose === ''outside'' && isMenuTarget) {');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('');
  FPack.Add('        // Tab navigation through the dropdown menu or events from contained inputs shouldn''t close the menu');
  FPack.Add('        if (context._menu.contains(event.target) && (event.type === ''keyup'' && event.key === TAB_KEY$1 || /input|select|option|textarea|form/i.test(event.target.tagName))) {');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('        const relatedTarget = {');
  FPack.Add('          relatedTarget: context._element');
  FPack.Add('        };');
  FPack.Add('        if (event.type === ''click'') {');
  FPack.Add('          relatedTarget.clickEvent = event;');
  FPack.Add('        }');
  FPack.Add('        context._completeHide(relatedTarget);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    static dataApiKeydownHandler(event) {');
  FPack.Add('      // If not an UP | DOWN | ESCAPE key => not a dropdown command');
  FPack.Add('      // If input/textarea && if key is other than ESCAPE => not a dropdown command');
  FPack.Add('');
  FPack.Add('      const isInput = /input|textarea/i.test(event.target.tagName);');
  FPack.Add('      const isEscapeEvent = event.key === ESCAPE_KEY$2;');
  FPack.Add('      const isUpOrDownEvent = [ARROW_UP_KEY$1, ARROW_DOWN_KEY$1].includes(event.key);');
  FPack.Add('      if (!isUpOrDownEvent && !isEscapeEvent) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (isInput && !isEscapeEvent) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      event.preventDefault();');
  FPack.Add('');
  FPack.Add('      // TODO: v6 revert #37011 & change markup https://getbootstrap.com/docs/5.3/forms/input-group/');
  FPack.Add('      const getToggleButton = this.matches(SELECTOR_DATA_TOGGLE$3) ? this : SelectorEngine.prev(this, SELECTOR_DATA_TOGGLE$3)[0] || SelectorEngine.next(this, SELECTOR_DATA_TOGGLE$3)[0] || SelectorEngine.findOne(SELECTOR_DATA_TOGGLE$3, event.delegateT'+'arget.parentNode);');
  FPack.Add('      const instance = Dropdown.getOrCreateInstance(getToggleButton);');
  FPack.Add('      if (isUpOrDownEvent) {');
  FPack.Add('        event.stopPropagation();');
  FPack.Add('        instance.show();');
  FPack.Add('        instance._selectMenuItem(event);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (instance._isShown()) {');
  FPack.Add('        // else is escape and we check if it is shown');
  FPack.Add('        event.stopPropagation();');
  FPack.Add('        instance.hide();');
  FPack.Add('        getToggleButton.focus();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_KEYDOWN_DATA_API, SELECTOR_DATA_TOGGLE$3, Dropdown.dataApiKeydownHandler);');
  FPack.Add('  EventHandler.on(document, EVENT_KEYDOWN_DATA_API, SELECTOR_MENU, Dropdown.dataApiKeydownHandler);');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$3, Dropdown.clearMenus);');
  FPack.Add('  EventHandler.on(document, EVENT_KEYUP_DATA_API, Dropdown.clearMenus);');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$3, SELECTOR_DATA_TOGGLE$3, function (event) {');
  FPack.Add('    event.preventDefault();');
  FPack.Add('    Dropdown.getOrCreateInstance(this).toggle();');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Dropdown);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/backdrop.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$9 = ''backdrop'';');
  FPack.Add('  const CLASS_NAME_FADE$4 = ''fade'';');
  FPack.Add('  const CLASS_NAME_SHOW$5 = ''show'';');
  FPack.Add('  const EVENT_MOUSEDOWN = `mousedown.bs.${NAME$9}`;');
  FPack.Add('  const Default$8 = {');
  FPack.Add('    className: ''modal-backdrop'',');
  FPack.Add('    clickCallback: null,');
  FPack.Add('    isAnimated: false,');
  FPack.Add('    isVisible: true,');
  FPack.Add('    // if false, we use the backdrop helper without adding any element to the dom');
  FPack.Add('    rootElement: ''body'' // give the choice to place backdrop under different elements');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$8 = {');
  FPack.Add('    className: ''string'',');
  FPack.Add('    clickCallback: ''(function|null)'',');
  FPack.Add('    isAnimated: ''boolean'',');
  FPack.Add('    isVisible: ''boolean'',');
  FPack.Add('    rootElement: ''(element|string)''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Backdrop extends Config {');
  FPack.Add('    constructor(config) {');
  FPack.Add('      super();');
  FPack.Add('      this._config = this._getConfig(config);');
  FPack.Add('      this._isAppended = false;');
  FPack.Add('      this._element = null;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$8;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$8;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$9;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    show(callback) {');
  FPack.Add('      if (!this._config.isVisible) {');
  FPack.Add('        execute(callback);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._append();');
  FPack.Add('      const element = this._getElement();');
  FPack.Add('      if (this._config.isAnimated) {');
  FPack.Add('        reflow(element);');
  FPack.Add('      }');
  FPack.Add('      element.classList.add(CLASS_NAME_SHOW$5);');
  FPack.Add('      this._emulateAnimation(() => {');
  FPack.Add('        execute(callback);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    hide(callback) {');
  FPack.Add('      if (!this._config.isVisible) {');
  FPack.Add('        execute(callback);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._getElement().classList.remove(CLASS_NAME_SHOW$5);');
  FPack.Add('      this._emulateAnimation(() => {');
  FPack.Add('        this.dispose();');
  FPack.Add('        execute(callback);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      if (!this._isAppended) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      EventHandler.off(this._element, EVENT_MOUSEDOWN);');
  FPack.Add('      this._element.remove();');
  FPack.Add('      this._isAppended = false;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _getElement() {');
  FPack.Add('      if (!this._element) {');
  FPack.Add('        const backdrop = document.createElement(''div'');');
  FPack.Add('        backdrop.className = this._config.className;');
  FPack.Add('        if (this._config.isAnimated) {');
  FPack.Add('          backdrop.classList.add(CLASS_NAME_FADE$4);');
  FPack.Add('        }');
  FPack.Add('        this._element = backdrop;');
  FPack.Add('      }');
  FPack.Add('      return this._element;');
  FPack.Add('    }');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      // use getElement() with the default "body" to get a fresh Element on each instantiation');
  FPack.Add('      config.rootElement = getElement(config.rootElement);');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _append() {');
  FPack.Add('      if (this._isAppended) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const element = this._getElement();');
  FPack.Add('      this._config.rootElement.append(element);');
  FPack.Add('      EventHandler.on(element, EVENT_MOUSEDOWN, () => {');
  FPack.Add('        execute(this._config.clickCallback);');
  FPack.Add('      });');
  FPack.Add('      this._isAppended = true;');
  FPack.Add('    }');
  FPack.Add('    _emulateAnimation(callback) {');
  FPack.Add('      executeAfterTransition(callback, this._getElement(), this._config.isAnimated);');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/focustrap.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$8 = ''focustrap'';');
  FPack.Add('  const DATA_KEY$5 = ''bs.focustrap'';');
  FPack.Add('  const EVENT_KEY$5 = `.${DATA_KEY$5}`;');
  FPack.Add('  const EVENT_FOCUSIN$2 = `focusin${EVENT_KEY$5}`;');
  FPack.Add('  const EVENT_KEYDOWN_TAB = `keydown.tab${EVENT_KEY$5}`;');
  FPack.Add('  const TAB_KEY = ''Tab'';');
  FPack.Add('  const TAB_NAV_FORWARD = ''forward'';');
  FPack.Add('  const TAB_NAV_BACKWARD = ''backward'';');
  FPack.Add('  const Default$7 = {');
  FPack.Add('    autofocus: true,');
  FPack.Add('    trapElement: null // The element to trap focus inside of');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$7 = {');
  FPack.Add('    autofocus: ''boolean'',');
  FPack.Add('    trapElement: ''element''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class FocusTrap extends Config {');
  FPack.Add('    constructor(config) {');
  FPack.Add('      super();');
  FPack.Add('      this._config = this._getConfig(config);');
  FPack.Add('      this._isActive = false;');
  FPack.Add('      this._lastTabNavDirection = null;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$7;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$7;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$8;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    activate() {');
  FPack.Add('      if (this._isActive) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (this._config.autofocus) {');
  FPack.Add('        this._config.trapElement.focus();');
  FPack.Add('      }');
  FPack.Add('      EventHandler.off(document, EVENT_KEY$5); // guard against infinite focus loop');
  FPack.Add('      EventHandler.on(document, EVENT_FOCUSIN$2, event => this._handleFocusin(event));');
  FPack.Add('      EventHandler.on(document, EVENT_KEYDOWN_TAB, event => this._handleKeydown(event));');
  FPack.Add('      this._isActive = true;');
  FPack.Add('    }');
  FPack.Add('    deactivate() {');
  FPack.Add('      if (!this._isActive) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isActive = false;');
  FPack.Add('      EventHandler.off(document, EVENT_KEY$5);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _handleFocusin(event) {');
  FPack.Add('      const {');
  FPack.Add('        trapElement');
  FPack.Add('      } = this._config;');
  FPack.Add('      if (event.target === document || event.target === trapElement || trapElement.contains(event.target)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const elements = SelectorEngine.focusableChildren(trapElement);');
  FPack.Add('      if (elements.length === 0) {');
  FPack.Add('        trapElement.focus();');
  FPack.Add('      } else if (this._lastTabNavDirection === TAB_NAV_BACKWARD) {');
  FPack.Add('        elements[elements.length - 1].focus();');
  FPack.Add('      } else {');
  FPack.Add('        elements[0].focus();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _handleKeydown(event) {');
  FPack.Add('      if (event.key !== TAB_KEY) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._lastTabNavDirection = event.shiftKey ? TAB_NAV_BACKWARD : TAB_NAV_FORWARD;');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/scrollBar.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const SELECTOR_FIXED_CONTENT = ''.fixed-top, .fixed-bottom, .is-fixed, .sticky-top'';');
  FPack.Add('  const SELECTOR_STICKY_CONTENT = ''.sticky-top'';');
  FPack.Add('  const PROPERTY_PADDING = ''padding-right'';');
  FPack.Add('  const PROPERTY_MARGIN = ''margin-right'';');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class ScrollBarHelper {');
  FPack.Add('    constructor() {');
  FPack.Add('      this._element = document.body;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    getWidth() {');
  FPack.Add('      // https://developer.mozilla.org/en-US/docs/Web/API/Window/innerWidth#usage_notes');
  FPack.Add('      const documentWidth = document.documentElement.clientWidth;');
  FPack.Add('      return Math.abs(window.innerWidth - documentWidth);');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      const width = this.getWidth();');
  FPack.Add('      this._disableOverFlow();');
  FPack.Add('      // give padding to element to balance the hidden scrollbar width');
  FPack.Add('      this._setElementAttributes(this._element, PROPERTY_PADDING, calculatedValue => calculatedValue + width);');
  FPack.Add('      // trick: We adjust positive paddingRight and negative marginRight to sticky-top elements to keep showing fullwidth');
  FPack.Add('      this._setElementAttributes(SELECTOR_FIXED_CONTENT, PROPERTY_PADDING, calculatedValue => calculatedValue + width);');
  FPack.Add('      this._setElementAttributes(SELECTOR_STICKY_CONTENT, PROPERTY_MARGIN, calculatedValue => calculatedValue - width);');
  FPack.Add('    }');
  FPack.Add('    reset() {');
  FPack.Add('      this._resetElementAttributes(this._element, ''overflow'');');
  FPack.Add('      this._resetElementAttributes(this._element, PROPERTY_PADDING);');
  FPack.Add('      this._resetElementAttributes(SELECTOR_FIXED_CONTENT, PROPERTY_PADDING);');
  FPack.Add('      this._resetElementAttributes(SELECTOR_STICKY_CONTENT, PROPERTY_MARGIN);');
  FPack.Add('    }');
  FPack.Add('    isOverflowing() {');
  FPack.Add('      return this.getWidth() > 0;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _disableOverFlow() {');
  FPack.Add('      this._saveInitialAttribute(this._element, ''overflow'');');
  FPack.Add('      this._element.style.overflow = ''hidden'';');
  FPack.Add('    }');
  FPack.Add('    _setElementAttributes(selector, styleProperty, callback) {');
  FPack.Add('      const scrollbarWidth = this.getWidth();');
  FPack.Add('      const manipulationCallBack = element => {');
  FPack.Add('        if (element !== this._element && window.innerWidth > element.clientWidth + scrollbarWidth) {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        this._saveInitialAttribute(element, styleProperty);');
  FPack.Add('        const calculatedValue = window.getComputedStyle(element).getPropertyValue(styleProperty);');
  FPack.Add('        element.style.setProperty(styleProperty, `${callback(Number.parseFloat(calculatedValue))}px`);');
  FPack.Add('      };');
  FPack.Add('      this._applyManipulationCallback(selector, manipulationCallBack);');
  FPack.Add('    }');
  FPack.Add('    _saveInitialAttribute(element, styleProperty) {');
  FPack.Add('      const actualValue = element.style.getPropertyValue(styleProperty);');
  FPack.Add('      if (actualValue) {');
  FPack.Add('        Manipulator.setDataAttribute(element, styleProperty, actualValue);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _resetElementAttributes(selector, styleProperty) {');
  FPack.Add('      const manipulationCallBack = element => {');
  FPack.Add('        const value = Manipulator.getDataAttribute(element, styleProperty);');
  FPack.Add('        // We only want to remove the property if the value is `null`; the value can also be zero');
  FPack.Add('        if (value === null) {');
  FPack.Add('          element.style.removeProperty(styleProperty);');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        Manipulator.removeDataAttribute(element, styleProperty);');
  FPack.Add('        element.style.setProperty(styleProperty, value);');
  FPack.Add('      };');
  FPack.Add('      this._applyManipulationCallback(selector, manipulationCallBack);');
  FPack.Add('    }');
  FPack.Add('    _applyManipulationCallback(selector, callBack) {');
  FPack.Add('      if (isElement(selector)) {');
  FPack.Add('        callBack(selector);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      for (const sel of SelectorEngine.find(selector, this._element)) {');
  FPack.Add('        callBack(sel);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap modal.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
end;

procedure TBootstrapJS.BootstrapJS_7;
begin
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$7 = ''modal'';');
  FPack.Add('  const DATA_KEY$4 = ''bs.modal'';');
  FPack.Add('  const EVENT_KEY$4 = `.${DATA_KEY$4}`;');
  FPack.Add('  const DATA_API_KEY$2 = ''.data-api'';');
  FPack.Add('  const ESCAPE_KEY$1 = ''Escape'';');
  FPack.Add('  const EVENT_HIDE$4 = `hide${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_HIDE_PREVENTED$1 = `hidePrevented${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_HIDDEN$4 = `hidden${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_SHOW$4 = `show${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_SHOWN$4 = `shown${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_RESIZE$1 = `resize${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_CLICK_DISMISS = `click.dismiss${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_MOUSEDOWN_DISMISS = `mousedown.dismiss${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_KEYDOWN_DISMISS$1 = `keydown.dismiss${EVENT_KEY$4}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API$2 = `click${EVENT_KEY$4}${DATA_API_KEY$2}`;');
  FPack.Add('  const CLASS_NAME_OPEN = ''modal-open'';');
  FPack.Add('  const CLASS_NAME_FADE$3 = ''fade'';');
  FPack.Add('  const CLASS_NAME_SHOW$4 = ''show'';');
  FPack.Add('  const CLASS_NAME_STATIC = ''modal-static'';');
  FPack.Add('  const OPEN_SELECTOR$1 = ''.modal.show'';');
  FPack.Add('  const SELECTOR_DIALOG = ''.modal-dialog'';');
  FPack.Add('  const SELECTOR_MODAL_BODY = ''.modal-body'';');
  FPack.Add('  const SELECTOR_DATA_TOGGLE$2 = ''[data-bs-toggle="modal"]'';');
  FPack.Add('  const Default$6 = {');
  FPack.Add('    backdrop: true,');
  FPack.Add('    focus: true,');
  FPack.Add('    keyboard: true');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$6 = {');
  FPack.Add('    backdrop: ''(boolean|string)'',');
  FPack.Add('    focus: ''boolean'',');
  FPack.Add('    keyboard: ''boolean''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Modal extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._dialog = SelectorEngine.findOne(SELECTOR_DIALOG, this._element);');
  FPack.Add('      this._backdrop = this._initializeBackDrop();');
  FPack.Add('      this._focustrap = this._initializeFocusTrap();');
  FPack.Add('      this._isShown = false;');
  FPack.Add('      this._isTransitioning = false;');
  FPack.Add('      this._scrollBar = new ScrollBarHelper();');
  FPack.Add('      this._addEventListeners();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$6;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$6;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$7;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    toggle(relatedTarget) {');
  FPack.Add('      return this._isShown ? this.hide() : this.show(relatedTarget);');
  FPack.Add('    }');
  FPack.Add('    show(relatedTarget) {');
  FPack.Add('      if (this._isShown || this._isTransitioning) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$4, {');
  FPack.Add('        relatedTarget');
  FPack.Add('      });');
  FPack.Add('      if (showEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isShown = true;');
  FPack.Add('      this._isTransitioning = true;');
  FPack.Add('      this._scrollBar.hide();');
  FPack.Add('      document.body.classList.add(CLASS_NAME_OPEN);');
  FPack.Add('      this._adjustDialog();');
  FPack.Add('      this._backdrop.show(() => this._showElement(relatedTarget));');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      if (!this._isShown || this._isTransitioning) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$4);');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isShown = false;');
  FPack.Add('      this._isTransitioning = true;');
  FPack.Add('      this._focustrap.deactivate();');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_SHOW$4);');
  FPack.Add('      this._queueCallback(() => this._hideModal(), this._element, this._isAnimated());');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      EventHandler.off(window, EVENT_KEY$4);');
  FPack.Add('      EventHandler.off(this._dialog, EVENT_KEY$4);');
  FPack.Add('      this._backdrop.dispose();');
  FPack.Add('      this._focustrap.deactivate();');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('    handleUpdate() {');
  FPack.Add('      this._adjustDialog();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _initializeBackDrop() {');
  FPack.Add('      return new Backdrop({');
  FPack.Add('        isVisible: Boolean(this._config.backdrop),');
  FPack.Add('        // ''static'' option will be translated to true, and booleans will keep their value,');
  FPack.Add('        isAnimated: this._isAnimated()');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _initializeFocusTrap() {');
  FPack.Add('      return new FocusTrap({');
  FPack.Add('        trapElement: this._element');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _showElement(relatedTarget) {');
  FPack.Add('      // try to append dynamic modal');
  FPack.Add('      if (!document.body.contains(this._element)) {');
  FPack.Add('        document.body.append(this._element);');
  FPack.Add('      }');
  FPack.Add('      this._element.style.display = ''block'';');
  FPack.Add('      this._element.removeAttribute(''aria-hidden'');');
  FPack.Add('      this._element.setAttribute(''aria-modal'', true);');
  FPack.Add('      this._element.setAttribute(''role'', ''dialog'');');
  FPack.Add('      this._element.scrollTop = 0;');
  FPack.Add('      const modalBody = SelectorEngine.findOne(SELECTOR_MODAL_BODY, this._dialog);');
  FPack.Add('      if (modalBody) {');
  FPack.Add('        modalBody.scrollTop = 0;');
  FPack.Add('      }');
  FPack.Add('      reflow(this._element);');
  FPack.Add('      this._element.classList.add(CLASS_NAME_SHOW$4);');
  FPack.Add('      const transitionComplete = () => {');
  FPack.Add('        if (this._config.focus) {');
  FPack.Add('          this._focustrap.activate();');
  FPack.Add('        }');
  FPack.Add('        this._isTransitioning = false;');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_SHOWN$4, {');
  FPack.Add('          relatedTarget');
  FPack.Add('        });');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(transitionComplete, this._dialog, this._isAnimated());');
  FPack.Add('    }');
  FPack.Add('    _addEventListeners() {');
  FPack.Add('      EventHandler.on(this._element, EVENT_KEYDOWN_DISMISS$1, event => {');
  FPack.Add('        if (event.key !== ESCAPE_KEY$1) {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (this._config.keyboard) {');
  FPack.Add('          this.hide();');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        this._triggerBackdropTransition();');
  FPack.Add('      });');
  FPack.Add('      EventHandler.on(window, EVENT_RESIZE$1, () => {');
  FPack.Add('        if (this._isShown && !this._isTransitioning) {');
  FPack.Add('          this._adjustDialog();');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('      EventHandler.on(this._element, EVENT_MOUSEDOWN_DISMISS, event => {');
  FPack.Add('        // a bad trick to segregate clicks that may start inside dialog but end outside, and avoid listen to scrollbar clicks');
  FPack.Add('        EventHandler.one(this._element, EVENT_CLICK_DISMISS, event2 => {');
  FPack.Add('          if (this._element !== event.target || this._element !== event2.target) {');
  FPack.Add('            return;');
  FPack.Add('          }');
  FPack.Add('          if (this._config.backdrop === ''static'') {');
  FPack.Add('            this._triggerBackdropTransition();');
  FPack.Add('            return;');
  FPack.Add('          }');
  FPack.Add('          if (this._config.backdrop) {');
  FPack.Add('            this.hide();');
  FPack.Add('          }');
  FPack.Add('        });');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _hideModal() {');
  FPack.Add('      this._element.style.display = ''none'';');
  FPack.Add('      this._element.setAttribute(''aria-hidden'', true);');
  FPack.Add('      this._element.removeAttribute(''aria-modal'');');
  FPack.Add('      this._element.removeAttribute(''role'');');
  FPack.Add('      this._isTransitioning = false;');
  FPack.Add('      this._backdrop.hide(() => {');
  FPack.Add('        document.body.classList.remove(CLASS_NAME_OPEN);');
  FPack.Add('        this._resetAdjustments();');
  FPack.Add('        this._scrollBar.reset();');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_HIDDEN$4);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _isAnimated() {');
  FPack.Add('      return this._element.classList.contains(CLASS_NAME_FADE$3);');
  FPack.Add('    }');
  FPack.Add('    _triggerBackdropTransition() {');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED$1);');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const isModalOverflowing = this._element.scrollHeight > document.documentElement.clientHeight;');
  FPack.Add('      const initialOverflowY = this._element.style.overflowY;');
  FPack.Add('      // return if the following background transition hasn''t yet completed');
  FPack.Add('      if (initialOverflowY === ''hidden'' || this._element.classList.contains(CLASS_NAME_STATIC)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (!isModalOverflowing) {');
  FPack.Add('        this._element.style.overflowY = ''hidden'';');
  FPack.Add('      }');
  FPack.Add('      this._element.classList.add(CLASS_NAME_STATIC);');
  FPack.Add('      this._queueCallback(() => {');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_STATIC);');
  FPack.Add('        this._queueCallback(() => {');
  FPack.Add('          this._element.style.overflowY = initialOverflowY;');
  FPack.Add('        }, this._dialog);');
  FPack.Add('      }, this._dialog);');
  FPack.Add('      this._element.focus();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    /**');
  FPack.Add('     * The following methods are used to handle overflowing modals');
  FPack.Add('     */');
  FPack.Add('');
  FPack.Add('    _adjustDialog() {');
  FPack.Add('      const isModalOverflowing = this._element.scrollHeight > document.documentElement.clientHeight;');
  FPack.Add('      const scrollbarWidth = this._scrollBar.getWidth();');
  FPack.Add('      const isBodyOverflowing = scrollbarWidth > 0;');
  FPack.Add('      if (isBodyOverflowing && !isModalOverflowing) {');
  FPack.Add('        const property = isRTL() ? ''paddingLeft'' : ''paddingRight'';');
  FPack.Add('        this._element.style[property] = `${scrollbarWidth}px`;');
  FPack.Add('      }');
  FPack.Add('      if (!isBodyOverflowing && isModalOverflowing) {');
  FPack.Add('        const property = isRTL() ? ''paddingRight'' : ''paddingLeft'';');
  FPack.Add('        this._element.style[property] = `${scrollbarWidth}px`;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _resetAdjustments() {');
  FPack.Add('      this._element.style.paddingLeft = '''';');
  FPack.Add('      this._element.style.paddingRight = '''';');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config, relatedTarget) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Modal.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (typeof data[config] === ''undefined'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config](relatedTarget);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$2, SELECTOR_DATA_TOGGLE$2, function (event) {');
  FPack.Add('    const target = SelectorEngine.getElementFromSelector(this);');
  FPack.Add('    if ([''A'', ''AREA''].includes(this.tagName)) {');
  FPack.Add('      event.preventDefault();');
  FPack.Add('    }');
  FPack.Add('    EventHandler.one(target, EVENT_SHOW$4, showEvent => {');
  FPack.Add('      if (showEvent.defaultPrevented) {');
  FPack.Add('        // only register focus restorer if modal will actually get shown');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      EventHandler.one(target, EVENT_HIDDEN$4, () => {');
  FPack.Add('        if (isVisible(this)) {');
  FPack.Add('          this.focus();');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    });');
  FPack.Add('');
  FPack.Add('    // avoid conflict when clicking modal toggler while another one is open');
  FPack.Add('    const alreadyOpen = SelectorEngine.findOne(OPEN_SELECTOR$1);');
  FPack.Add('    if (alreadyOpen) {');
  FPack.Add('      Modal.getInstance(alreadyOpen).hide();');
  FPack.Add('    }');
  FPack.Add('    const data = Modal.getOrCreateInstance(target);');
  FPack.Add('    data.toggle(this);');
  FPack.Add('  });');
  FPack.Add('  enableDismissTrigger(Modal);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Modal);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap offcanvas.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$6 = ''offcanvas'';');
  FPack.Add('  const DATA_KEY$3 = ''bs.offcanvas'';');
  FPack.Add('  const EVENT_KEY$3 = `.${DATA_KEY$3}`;');
  FPack.Add('  const DATA_API_KEY$1 = ''.data-api'';');
  FPack.Add('  const EVENT_LOAD_DATA_API$2 = `load${EVENT_KEY$3}${DATA_API_KEY$1}`;');
  FPack.Add('  const ESCAPE_KEY = ''Escape'';');
  FPack.Add('  const CLASS_NAME_SHOW$3 = ''show'';');
  FPack.Add('  const CLASS_NAME_SHOWING$1 = ''showing'';');
  FPack.Add('  const CLASS_NAME_HIDING = ''hiding'';');
  FPack.Add('  const CLASS_NAME_BACKDROP = ''offcanvas-backdrop'';');
  FPack.Add('  const OPEN_SELECTOR = ''.offcanvas.show'';');
  FPack.Add('  const EVENT_SHOW$3 = `show${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_SHOWN$3 = `shown${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_HIDE$3 = `hide${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_HIDE_PREVENTED = `hidePrevented${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_HIDDEN$3 = `hidden${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_RESIZE = `resize${EVENT_KEY$3}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API$1 = `click${EVENT_KEY$3}${DATA_API_KEY$1}`;');
  FPack.Add('  const EVENT_KEYDOWN_DISMISS = `keydown.dismiss${EVENT_KEY$3}`;');
  FPack.Add('  const SELECTOR_DATA_TOGGLE$1 = ''[data-bs-toggle="offcanvas"]'';');
  FPack.Add('  const Default$5 = {');
  FPack.Add('    backdrop: true,');
  FPack.Add('    keyboard: true,');
  FPack.Add('    scroll: false');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$5 = {');
  FPack.Add('    backdrop: ''(boolean|string)'',');
  FPack.Add('    keyboard: ''boolean'',');
  FPack.Add('    scroll: ''boolean''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Offcanvas extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._isShown = false;');
  FPack.Add('      this._backdrop = this._initializeBackDrop();');
  FPack.Add('      this._focustrap = this._initializeFocusTrap();');
  FPack.Add('      this._addEventListeners();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$5;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$5;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$6;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    toggle(relatedTarget) {');
  FPack.Add('      return this._isShown ? this.hide() : this.show(relatedTarget);');
  FPack.Add('    }');
  FPack.Add('    show(relatedTarget) {');
  FPack.Add('      if (this._isShown) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$3, {');
  FPack.Add('        relatedTarget');
  FPack.Add('      });');
  FPack.Add('      if (showEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isShown = true;');
  FPack.Add('      this._backdrop.show();');
  FPack.Add('      if (!this._config.scroll) {');
  FPack.Add('        new ScrollBarHelper().hide();');
  FPack.Add('      }');
  FPack.Add('      this._element.setAttribute(''aria-modal'', true);');
  FPack.Add('      this._element.setAttribute(''role'', ''dialog'');');
  FPack.Add('      this._element.classList.add(CLASS_NAME_SHOWING$1);');
  FPack.Add('      const completeCallBack = () => {');
  FPack.Add('        if (!this._config.scroll || this._config.backdrop) {');
  FPack.Add('          this._focustrap.activate();');
  FPack.Add('        }');
  FPack.Add('        this._element.classList.add(CLASS_NAME_SHOW$3);');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_SHOWING$1);');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_SHOWN$3, {');
  FPack.Add('          relatedTarget');
  FPack.Add('        });');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(completeCallBack, this._element, true);');
  FPack.Add('    }');
end;

procedure TBootstrapJS.BootstrapJS_8;
begin
  FPack.Add('    hide() {');
  FPack.Add('      if (!this._isShown) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$3);');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._focustrap.deactivate();');
  FPack.Add('      this._element.blur();');
  FPack.Add('      this._isShown = false;');
  FPack.Add('      this._element.classList.add(CLASS_NAME_HIDING);');
  FPack.Add('      this._backdrop.hide();');
  FPack.Add('      const completeCallback = () => {');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_SHOW$3, CLASS_NAME_HIDING);');
  FPack.Add('        this._element.removeAttribute(''aria-modal'');');
  FPack.Add('        this._element.removeAttribute(''role'');');
  FPack.Add('        if (!this._config.scroll) {');
  FPack.Add('          new ScrollBarHelper().reset();');
  FPack.Add('        }');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_HIDDEN$3);');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(completeCallback, this._element, true);');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      this._backdrop.dispose();');
  FPack.Add('      this._focustrap.deactivate();');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _initializeBackDrop() {');
  FPack.Add('      const clickCallback = () => {');
  FPack.Add('        if (this._config.backdrop === ''static'') {');
  FPack.Add('          EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED);');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        this.hide();');
  FPack.Add('      };');
  FPack.Add('');
  FPack.Add('      // ''static'' option will be translated to true, and booleans will keep their value');
  FPack.Add('      const isVisible = Boolean(this._config.backdrop);');
  FPack.Add('      return new Backdrop({');
  FPack.Add('        className: CLASS_NAME_BACKDROP,');
  FPack.Add('        isVisible,');
  FPack.Add('        isAnimated: true,');
  FPack.Add('        rootElement: this._element.parentNode,');
  FPack.Add('        clickCallback: isVisible ? clickCallback : null');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _initializeFocusTrap() {');
  FPack.Add('      return new FocusTrap({');
  FPack.Add('        trapElement: this._element');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _addEventListeners() {');
  FPack.Add('      EventHandler.on(this._element, EVENT_KEYDOWN_DISMISS, event => {');
  FPack.Add('        if (event.key !== ESCAPE_KEY) {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (this._config.keyboard) {');
  FPack.Add('          this.hide();');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Offcanvas.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (data[config] === undefined || config.startsWith(''_'') || config === ''constructor'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config](this);');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API$1, SELECTOR_DATA_TOGGLE$1, function (event) {');
  FPack.Add('    const target = SelectorEngine.getElementFromSelector(this);');
  FPack.Add('    if ([''A'', ''AREA''].includes(this.tagName)) {');
  FPack.Add('      event.preventDefault();');
  FPack.Add('    }');
  FPack.Add('    if (isDisabled(this)) {');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    EventHandler.one(target, EVENT_HIDDEN$3, () => {');
  FPack.Add('      // focus on trigger when it is closed');
  FPack.Add('      if (isVisible(this)) {');
  FPack.Add('        this.focus();');
  FPack.Add('      }');
  FPack.Add('    });');
  FPack.Add('');
  FPack.Add('    // avoid conflict when clicking a toggler of an offcanvas, while another is open');
  FPack.Add('    const alreadyOpen = SelectorEngine.findOne(OPEN_SELECTOR);');
  FPack.Add('    if (alreadyOpen && alreadyOpen !== target) {');
  FPack.Add('      Offcanvas.getInstance(alreadyOpen).hide();');
  FPack.Add('    }');
  FPack.Add('    const data = Offcanvas.getOrCreateInstance(target);');
  FPack.Add('    data.toggle(this);');
  FPack.Add('  });');
  FPack.Add('  EventHandler.on(window, EVENT_LOAD_DATA_API$2, () => {');
  FPack.Add('    for (const selector of SelectorEngine.find(OPEN_SELECTOR)) {');
  FPack.Add('      Offcanvas.getOrCreateInstance(selector).show();');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('  EventHandler.on(window, EVENT_RESIZE, () => {');
  FPack.Add('    for (const element of SelectorEngine.find(''[aria-modal][class*=show][class*=offcanvas-]'')) {');
  FPack.Add('      if (getComputedStyle(element).position !== ''fixed'') {');
  FPack.Add('        Offcanvas.getOrCreateInstance(element).hide();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('  enableDismissTrigger(Offcanvas);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Offcanvas);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/sanitizer.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  // js-docs-start allow-list');
  FPack.Add('  const ARIA_ATTRIBUTE_PATTERN = /^aria-[\w-]*$/i;');
  FPack.Add('  const DefaultAllowlist = {');
  FPack.Add('    // Global attributes allowed on any supplied element below.');
  FPack.Add('    ''*'': [''class'', ''dir'', ''id'', ''lang'', ''role'', ARIA_ATTRIBUTE_PATTERN],');
  FPack.Add('    a: [''target'', ''href'', ''title'', ''rel''],');
  FPack.Add('    area: [],');
  FPack.Add('    b: [],');
  FPack.Add('    br: [],');
  FPack.Add('    col: [],');
  FPack.Add('    code: [],');
  FPack.Add('    dd: [],');
  FPack.Add('    div: [],');
  FPack.Add('    dl: [],');
  FPack.Add('    dt: [],');
  FPack.Add('    em: [],');
  FPack.Add('    hr: [],');
  FPack.Add('    h1: [],');
  FPack.Add('    h2: [],');
  FPack.Add('    h3: [],');
  FPack.Add('    h4: [],');
  FPack.Add('    h5: [],');
  FPack.Add('    h6: [],');
  FPack.Add('    i: [],');
  FPack.Add('    img: [''src'', ''srcset'', ''alt'', ''title'', ''width'', ''height''],');
  FPack.Add('    li: [],');
  FPack.Add('    ol: [],');
  FPack.Add('    p: [],');
  FPack.Add('    pre: [],');
  FPack.Add('    s: [],');
  FPack.Add('    small: [],');
  FPack.Add('    span: [],');
  FPack.Add('    sub: [],');
  FPack.Add('    sup: [],');
  FPack.Add('    strong: [],');
  FPack.Add('    u: [],');
  FPack.Add('    ul: []');
  FPack.Add('  };');
  FPack.Add('  // js-docs-end allow-list');
  FPack.Add('');
  FPack.Add('  const uriAttributes = new Set([''background'', ''cite'', ''href'', ''itemtype'', ''longdesc'', ''poster'', ''src'', ''xlink:href'']);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * A pattern that recognizes URLs that are safe wrt. XSS in URL navigation');
  FPack.Add('   * contexts.');
  FPack.Add('   *');
  FPack.Add('   * Shout-out to Angular https://github.com/angular/angular/blob/15.2.8/packages/core/src/sanitization/url_sanitizer.ts#L38');
  FPack.Add('   */');
  FPack.Add('  // eslint-disable-next-line unicorn/better-regex');
  FPack.Add('  const SAFE_URL_PATTERN = /^(?!javascript:)(?:[a-z0-9+.-]+:|[^&:/?#]*(?:[/?#]|$))/i;');
  FPack.Add('  const allowedAttribute = (attribute, allowedAttributeList) => {');
  FPack.Add('    const attributeName = attribute.nodeName.toLowerCase();');
  FPack.Add('    if (allowedAttributeList.includes(attributeName)) {');
  FPack.Add('      if (uriAttributes.has(attributeName)) {');
  FPack.Add('        return Boolean(SAFE_URL_PATTERN.test(attribute.nodeValue));');
  FPack.Add('      }');
  FPack.Add('      return true;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Check if a regular expression validates the attribute.');
  FPack.Add('    return allowedAttributeList.filter(attributeRegex => attributeRegex instanceof RegExp).some(regex => regex.test(attributeName));');
  FPack.Add('  };');
  FPack.Add('  function sanitizeHtml(unsafeHtml, allowList, sanitizeFunction) {');
  FPack.Add('    if (!unsafeHtml.length) {');
  FPack.Add('      return unsafeHtml;');
  FPack.Add('    }');
  FPack.Add('    if (sanitizeFunction && typeof sanitizeFunction === ''function'') {');
  FPack.Add('      return sanitizeFunction(unsafeHtml);');
  FPack.Add('    }');
  FPack.Add('    const domParser = new window.DOMParser();');
  FPack.Add('    const createdDocument = domParser.parseFromString(unsafeHtml, ''text/html'');');
  FPack.Add('    const elements = [].concat(...createdDocument.body.querySelectorAll(''*''));');
  FPack.Add('    for (const element of elements) {');
  FPack.Add('      const elementName = element.nodeName.toLowerCase();');
  FPack.Add('      if (!Object.keys(allowList).includes(elementName)) {');
  FPack.Add('        element.remove();');
  FPack.Add('        continue;');
  FPack.Add('      }');
  FPack.Add('      const attributeList = [].concat(...element.attributes);');
  FPack.Add('      const allowedAttributes = [].concat(allowList[''*''] || [], allowList[elementName] || []);');
  FPack.Add('      for (const attribute of attributeList) {');
  FPack.Add('        if (!allowedAttribute(attribute, allowedAttributes)) {');
  FPack.Add('          element.removeAttribute(attribute.nodeName);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    return createdDocument.body.innerHTML;');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap util/template-factory.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$5 = ''TemplateFactory'';');
  FPack.Add('  const Default$4 = {');
  FPack.Add('    allowList: DefaultAllowlist,');
  FPack.Add('    content: {},');
  FPack.Add('    // { selector : text ,  selector2 : text2 , }');
  FPack.Add('    extraClass: '''',');
  FPack.Add('    html: false,');
  FPack.Add('    sanitize: true,');
  FPack.Add('    sanitizeFn: null,');
  FPack.Add('    template: ''<div></div>''');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$4 = {');
  FPack.Add('    allowList: ''object'',');
  FPack.Add('    content: ''object'',');
  FPack.Add('    extraClass: ''(string|function)'',');
  FPack.Add('    html: ''boolean'',');
  FPack.Add('    sanitize: ''boolean'',');
  FPack.Add('    sanitizeFn: ''(null|function)'',');
  FPack.Add('    template: ''string''');
  FPack.Add('  };');
  FPack.Add('  const DefaultContentType = {');
  FPack.Add('    entry: ''(string|element|function|null)'',');
  FPack.Add('    selector: ''(string|element)''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class TemplateFactory extends Config {');
  FPack.Add('    constructor(config) {');
  FPack.Add('      super();');
  FPack.Add('      this._config = this._getConfig(config);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$4;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$4;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$5;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    getContent() {');
  FPack.Add('      return Object.values(this._config.content).map(config => this._resolvePossibleFunction(config)).filter(Boolean);');
  FPack.Add('    }');
  FPack.Add('    hasContent() {');
  FPack.Add('      return this.getContent().length > 0;');
  FPack.Add('    }');
  FPack.Add('    changeContent(content) {');
  FPack.Add('      this._checkContent(content);');
  FPack.Add('      this._config.content = {');
  FPack.Add('        ...this._config.content,');
  FPack.Add('        ...content');
  FPack.Add('      };');
  FPack.Add('      return this;');
  FPack.Add('    }');
  FPack.Add('    toHtml() {');
  FPack.Add('      const templateWrapper = document.createElement(''div'');');
  FPack.Add('      templateWrapper.innerHTML = this._maybeSanitize(this._config.template);');
  FPack.Add('      for (const [selector, text] of Object.entries(this._config.content)) {');
  FPack.Add('        this._setContent(templateWrapper, text, selector);');
  FPack.Add('      }');
  FPack.Add('      const template = templateWrapper.children[0];');
  FPack.Add('      const extraClass = this._resolvePossibleFunction(this._config.extraClass);');
  FPack.Add('      if (extraClass) {');
  FPack.Add('        template.classList.add(...extraClass.split('' ''));');
  FPack.Add('      }');
  FPack.Add('      return template;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _typeCheckConfig(config) {');
  FPack.Add('      super._typeCheckConfig(config);');
  FPack.Add('      this._checkContent(config.content);');
  FPack.Add('    }');
  FPack.Add('    _checkContent(arg) {');
  FPack.Add('      for (const [selector, content] of Object.entries(arg)) {');
  FPack.Add('        super._typeCheckConfig({');
  FPack.Add('          selector,');
  FPack.Add('          entry: content');
  FPack.Add('        }, DefaultContentType);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _setContent(template, content, selector) {');
  FPack.Add('      const templateElement = SelectorEngine.findOne(selector, template);');
  FPack.Add('      if (!templateElement) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      content = this._resolvePossibleFunction(content);');
  FPack.Add('      if (!content) {');
  FPack.Add('        templateElement.remove();');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (isElement(content)) {');
  FPack.Add('        this._putElementInTemplate(getElement(content), templateElement);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (this._config.html) {');
  FPack.Add('        templateElement.innerHTML = this._maybeSanitize(content);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      templateElement.textContent = content;');
  FPack.Add('    }');
  FPack.Add('    _maybeSanitize(arg) {');
  FPack.Add('      return this._config.sanitize ? sanitizeHtml(arg, this._config.allowList, this._config.sanitizeFn) : arg;');
  FPack.Add('    }');
  FPack.Add('    _resolvePossibleFunction(arg) {');
  FPack.Add('      return execute(arg, [this]);');
  FPack.Add('    }');
  FPack.Add('    _putElementInTemplate(element, templateElement) {');
  FPack.Add('      if (this._config.html) {');
  FPack.Add('        templateElement.innerHTML = '''';');
  FPack.Add('        templateElement.append(element);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      templateElement.textContent = element.textContent;');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap tooltip.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$4 = ''tooltip'';');
  FPack.Add('  const DISALLOWED_ATTRIBUTES = new Set([''sanitize'', ''allowList'', ''sanitizeFn'']);');
  FPack.Add('  const CLASS_NAME_FADE$2 = ''fade'';');
  FPack.Add('  const CLASS_NAME_MODAL = ''modal'';');
  FPack.Add('  const CLASS_NAME_SHOW$2 = ''show'';');
  FPack.Add('  const SELECTOR_TOOLTIP_INNER = ''.tooltip-inner'';');
  FPack.Add('  const SELECTOR_MODAL = `.${CLASS_NAME_MODAL}`;');
  FPack.Add('  const EVENT_MODAL_HIDE = ''hide.bs.modal'';');
  FPack.Add('  const TRIGGER_HOVER = ''hover'';');
  FPack.Add('  const TRIGGER_FOCUS = ''focus'';');
  FPack.Add('  const TRIGGER_CLICK = ''click'';');
  FPack.Add('  const TRIGGER_MANUAL = ''manual'';');
  FPack.Add('  const EVENT_HIDE$2 = ''hide'';');
  FPack.Add('  const EVENT_HIDDEN$2 = ''hidden'';');
  FPack.Add('  const EVENT_SHOW$2 = ''show'';');
  FPack.Add('  const EVENT_SHOWN$2 = ''shown'';');
  FPack.Add('  const EVENT_INSERTED = ''inserted'';');
  FPack.Add('  const EVENT_CLICK$1 = ''click'';');
  FPack.Add('  const EVENT_FOCUSIN$1 = ''focusin'';');
  FPack.Add('  const EVENT_FOCUSOUT$1 = ''focusout'';');
  FPack.Add('  const EVENT_MOUSEENTER = ''mouseenter'';');
  FPack.Add('  const EVENT_MOUSELEAVE = ''mouseleave'';');
  FPack.Add('  const AttachmentMap = {');
  FPack.Add('    AUTO: ''auto'',');
  FPack.Add('    TOP: ''top'',');
  FPack.Add('    RIGHT: isRTL() ? ''left'' : ''right'',');
end;

procedure TBootstrapJS.BootstrapJS_9;
begin
  FPack.Add('    BOTTOM: ''bottom'',');
  FPack.Add('    LEFT: isRTL() ? ''right'' : ''left''');
  FPack.Add('  };');
  FPack.Add('  const Default$3 = {');
  FPack.Add('    allowList: DefaultAllowlist,');
  FPack.Add('    animation: true,');
  FPack.Add('    boundary: ''clippingParents'',');
  FPack.Add('    container: false,');
  FPack.Add('    customClass: '''',');
  FPack.Add('    delay: 0,');
  FPack.Add('    fallbackPlacements: [''top'', ''right'', ''bottom'', ''left''],');
  FPack.Add('    html: false,');
  FPack.Add('    offset: [0, 6],');
  FPack.Add('    placement: ''top'',');
  FPack.Add('    popperConfig: null,');
  FPack.Add('    sanitize: true,');
  FPack.Add('    sanitizeFn: null,');
  FPack.Add('    selector: false,');
  FPack.Add('    template: ''<div class="tooltip" role="tooltip">'' + ''<div class="tooltip-arrow"></div>'' + ''<div class="tooltip-inner"></div>'' + ''</div>'',');
  FPack.Add('    title: '''',');
  FPack.Add('    trigger: ''hover focus''');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$3 = {');
  FPack.Add('    allowList: ''object'',');
  FPack.Add('    animation: ''boolean'',');
  FPack.Add('    boundary: ''(string|element)'',');
  FPack.Add('    container: ''(string|element|boolean)'',');
  FPack.Add('    customClass: ''(string|function)'',');
  FPack.Add('    delay: ''(number|object)'',');
  FPack.Add('    fallbackPlacements: ''array'',');
  FPack.Add('    html: ''boolean'',');
  FPack.Add('    offset: ''(array|string|function)'',');
  FPack.Add('    placement: ''(string|function)'',');
  FPack.Add('    popperConfig: ''(null|object|function)'',');
  FPack.Add('    sanitize: ''boolean'',');
  FPack.Add('    sanitizeFn: ''(null|function)'',');
  FPack.Add('    selector: ''(string|boolean)'',');
  FPack.Add('    template: ''string'',');
  FPack.Add('    title: ''(string|element|function)'',');
  FPack.Add('    trigger: ''string''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Tooltip extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      if (typeof Popper__namespace === ''undefined'') {');
  FPack.Add('        throw new TypeError(''Bootstrap\''s tooltips require Popper (https://popper.js.org)'');');
  FPack.Add('      }');
  FPack.Add('      super(element, config);');
  FPack.Add('');
  FPack.Add('      // Private');
  FPack.Add('      this._isEnabled = true;');
  FPack.Add('      this._timeout = 0;');
  FPack.Add('      this._isHovered = null;');
  FPack.Add('      this._activeTrigger = {};');
  FPack.Add('      this._popper = null;');
  FPack.Add('      this._templateFactory = null;');
  FPack.Add('      this._newContent = null;');
  FPack.Add('');
  FPack.Add('      // Protected');
  FPack.Add('      this.tip = null;');
  FPack.Add('      this._setListeners();');
  FPack.Add('      if (!this._config.selector) {');
  FPack.Add('        this._fixTitle();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$3;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$3;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$4;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    enable() {');
  FPack.Add('      this._isEnabled = true;');
  FPack.Add('    }');
  FPack.Add('    disable() {');
  FPack.Add('      this._isEnabled = false;');
  FPack.Add('    }');
  FPack.Add('    toggleEnabled() {');
  FPack.Add('      this._isEnabled = !this._isEnabled;');
  FPack.Add('    }');
  FPack.Add('    toggle() {');
  FPack.Add('      if (!this._isEnabled) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._activeTrigger.click = !this._activeTrigger.click;');
  FPack.Add('      if (this._isShown()) {');
  FPack.Add('        this._leave();');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._enter();');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      clearTimeout(this._timeout);');
  FPack.Add('      EventHandler.off(this._element.closest(SELECTOR_MODAL), EVENT_MODAL_HIDE, this._hideModalHandler);');
  FPack.Add('      if (this._element.getAttribute(''data-bs-original-title'')) {');
  FPack.Add('        this._element.setAttribute(''title'', this._element.getAttribute(''data-bs-original-title''));');
  FPack.Add('      }');
  FPack.Add('      this._disposePopper();');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('    show() {');
  FPack.Add('      if (this._element.style.display === ''none'') {');
  FPack.Add('        throw new Error(''Please use show on visible elements'');');
  FPack.Add('      }');
  FPack.Add('      if (!(this._isWithContent() && this._isEnabled)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const showEvent = EventHandler.trigger(this._element, this.constructor.eventName(EVENT_SHOW$2));');
  FPack.Add('      const shadowRoot = findShadowRoot(this._element);');
  FPack.Add('      const isInTheDom = (shadowRoot || this._element.ownerDocument.documentElement).contains(this._element);');
  FPack.Add('      if (showEvent.defaultPrevented || !isInTheDom) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // TODO: v6 remove this or make it optional');
  FPack.Add('      this._disposePopper();');
  FPack.Add('      const tip = this._getTipElement();');
  FPack.Add('      this._element.setAttribute(''aria-describedby'', tip.getAttribute(''id''));');
  FPack.Add('      const {');
  FPack.Add('        container');
  FPack.Add('      } = this._config;');
  FPack.Add('      if (!this._element.ownerDocument.documentElement.contains(this.tip)) {');
  FPack.Add('        container.append(tip);');
  FPack.Add('        EventHandler.trigger(this._element, this.constructor.eventName(EVENT_INSERTED));');
  FPack.Add('      }');
  FPack.Add('      this._popper = this._createPopper(tip);');
  FPack.Add('      tip.classList.add(CLASS_NAME_SHOW$2);');
  FPack.Add('');
  FPack.Add('      // If this is a touch-enabled device we add extra');
  FPack.Add('      // empty mouseover listeners to the body''s immediate children;');
  FPack.Add('      // only needed because of broken event delegation on iOS');
  FPack.Add('      // https://www.quirksmode.org/blog/archives/2014/02/mouse_event_bub.html');
  FPack.Add('      if (''ontouchstart'' in document.documentElement) {');
  FPack.Add('        for (const element of [].concat(...document.body.children)) {');
  FPack.Add('          EventHandler.on(element, ''mouseover'', noop);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      const complete = () => {');
  FPack.Add('        EventHandler.trigger(this._element, this.constructor.eventName(EVENT_SHOWN$2));');
  FPack.Add('        if (this._isHovered === false) {');
  FPack.Add('          this._leave();');
  FPack.Add('        }');
  FPack.Add('        this._isHovered = false;');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(complete, this.tip, this._isAnimated());');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      if (!this._isShown()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, this.constructor.eventName(EVENT_HIDE$2));');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const tip = this._getTipElement();');
  FPack.Add('      tip.classList.remove(CLASS_NAME_SHOW$2);');
  FPack.Add('');
  FPack.Add('      // If this is a touch-enabled device we remove the extra');
  FPack.Add('      // empty mouseover listeners we added for iOS support');
  FPack.Add('      if (''ontouchstart'' in document.documentElement) {');
  FPack.Add('        for (const element of [].concat(...document.body.children)) {');
  FPack.Add('          EventHandler.off(element, ''mouseover'', noop);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      this._activeTrigger[TRIGGER_CLICK] = false;');
  FPack.Add('      this._activeTrigger[TRIGGER_FOCUS] = false;');
  FPack.Add('      this._activeTrigger[TRIGGER_HOVER] = false;');
  FPack.Add('      this._isHovered = null; // it is a trick to support manual triggering');
  FPack.Add('');
  FPack.Add('      const complete = () => {');
  FPack.Add('        if (this._isWithActiveTrigger()) {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (!this._isHovered) {');
  FPack.Add('          this._disposePopper();');
  FPack.Add('        }');
  FPack.Add('        this._element.removeAttribute(''aria-describedby'');');
  FPack.Add('        EventHandler.trigger(this._element, this.constructor.eventName(EVENT_HIDDEN$2));');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(complete, this.tip, this._isAnimated());');
  FPack.Add('    }');
  FPack.Add('    update() {');
  FPack.Add('      if (this._popper) {');
  FPack.Add('        this._popper.update();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Protected');
  FPack.Add('    _isWithContent() {');
  FPack.Add('      return Boolean(this._getTitle());');
  FPack.Add('    }');
  FPack.Add('    _getTipElement() {');
  FPack.Add('      if (!this.tip) {');
  FPack.Add('        this.tip = this._createTipElement(this._newContent || this._getContentForTemplate());');
  FPack.Add('      }');
  FPack.Add('      return this.tip;');
  FPack.Add('    }');
  FPack.Add('    _createTipElement(content) {');
  FPack.Add('      const tip = this._getTemplateFactory(content).toHtml();');
  FPack.Add('');
  FPack.Add('      // TODO: remove this check in v6');
  FPack.Add('      if (!tip) {');
  FPack.Add('        return null;');
  FPack.Add('      }');
  FPack.Add('      tip.classList.remove(CLASS_NAME_FADE$2, CLASS_NAME_SHOW$2);');
  FPack.Add('      // TODO: v6 the following can be achieved with CSS only');
  FPack.Add('      tip.classList.add(`bs-${this.constructor.NAME}-auto`);');
  FPack.Add('      const tipId = getUID(this.constructor.NAME).toString();');
  FPack.Add('      tip.setAttribute(''id'', tipId);');
  FPack.Add('      if (this._isAnimated()) {');
  FPack.Add('        tip.classList.add(CLASS_NAME_FADE$2);');
  FPack.Add('      }');
  FPack.Add('      return tip;');
  FPack.Add('    }');
  FPack.Add('    setContent(content) {');
  FPack.Add('      this._newContent = content;');
  FPack.Add('      if (this._isShown()) {');
  FPack.Add('        this._disposePopper();');
  FPack.Add('        this.show();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _getTemplateFactory(content) {');
  FPack.Add('      if (this._templateFactory) {');
  FPack.Add('        this._templateFactory.changeContent(content);');
  FPack.Add('      } else {');
  FPack.Add('        this._templateFactory = new TemplateFactory({');
  FPack.Add('          ...this._config,');
  FPack.Add('          // the `content` var has to be after `this._config`');
  FPack.Add('          // to override config.content in case of popover');
  FPack.Add('          content,');
  FPack.Add('          extraClass: this._resolvePossibleFunction(this._config.customClass)');
  FPack.Add('        });');
  FPack.Add('      }');
  FPack.Add('      return this._templateFactory;');
  FPack.Add('    }');
  FPack.Add('    _getContentForTemplate() {');
  FPack.Add('      return {');
  FPack.Add('        [SELECTOR_TOOLTIP_INNER]: this._getTitle()');
  FPack.Add('      };');
  FPack.Add('    }');
  FPack.Add('    _getTitle() {');
  FPack.Add('      return this._resolvePossibleFunction(this._config.title) || this._element.getAttribute(''data-bs-original-title'');');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _initializeOnDelegatedTarget(event) {');
  FPack.Add('      return this.constructor.getOrCreateInstance(event.delegateTarget, this._getDelegateConfig());');
  FPack.Add('    }');
  FPack.Add('    _isAnimated() {');
  FPack.Add('      return this._config.animation || this.tip && this.tip.classList.contains(CLASS_NAME_FADE$2);');
  FPack.Add('    }');
  FPack.Add('    _isShown() {');
  FPack.Add('      return this.tip && this.tip.classList.contains(CLASS_NAME_SHOW$2);');
  FPack.Add('    }');
  FPack.Add('    _createPopper(tip) {');
  FPack.Add('      const placement = execute(this._config.placement, [this, tip, this._element]);');
  FPack.Add('      const attachment = AttachmentMap[placement.toUpperCase()];');
  FPack.Add('      return Popper__namespace.createPopper(this._element, tip, this._getPopperConfig(attachment));');
  FPack.Add('    }');
  FPack.Add('    _getOffset() {');
  FPack.Add('      const {');
  FPack.Add('        offset');
  FPack.Add('      } = this._config;');
  FPack.Add('      if (typeof offset === ''string'') {');
  FPack.Add('        return offset.split('','').map(value => Number.parseInt(value, 10));');
  FPack.Add('      }');
  FPack.Add('      if (typeof offset === ''function'') {');
  FPack.Add('        return popperData => offset(popperData, this._element);');
  FPack.Add('      }');
  FPack.Add('      return offset;');
  FPack.Add('    }');
  FPack.Add('    _resolvePossibleFunction(arg) {');
  FPack.Add('      return execute(arg, [this._element]);');
  FPack.Add('    }');
  FPack.Add('    _getPopperConfig(attachment) {');
  FPack.Add('      const defaultBsPopperConfig = {');
  FPack.Add('        placement: attachment,');
  FPack.Add('        modifiers: [{');
  FPack.Add('          name: ''flip'',');
  FPack.Add('          options: {');
  FPack.Add('            fallbackPlacements: this._config.fallbackPlacements');
  FPack.Add('          }');
  FPack.Add('        }, {');
  FPack.Add('          name: ''offset'',');
  FPack.Add('          options: {');
  FPack.Add('            offset: this._getOffset()');
  FPack.Add('          }');
  FPack.Add('        }, {');
  FPack.Add('          name: ''preventOverflow'',');
  FPack.Add('          options: {');
  FPack.Add('            boundary: this._config.boundary');
  FPack.Add('          }');
  FPack.Add('        }, {');
  FPack.Add('          name: ''arrow'',');
  FPack.Add('          options: {');
  FPack.Add('            element: `.${this.constructor.NAME}-arrow`');
  FPack.Add('          }');
  FPack.Add('        }, {');
  FPack.Add('          name: ''preSetPlacement'',');
  FPack.Add('          enabled: true,');
  FPack.Add('          phase: ''beforeMain'',');
  FPack.Add('          fn: data => {');
  FPack.Add('            // Pre-set Popper''s placement attribute in order to read the arrow sizes properly.');
  FPack.Add('            // Otherwise, Popper mixes up the width and height dimensions since the initial arrow style is for top placement');
  FPack.Add('            this._getTipElement().setAttribute(''data-popper-placement'', data.state.placement);');
  FPack.Add('          }');
  FPack.Add('        }]');
  FPack.Add('      };');
  FPack.Add('      return {');
  FPack.Add('        ...defaultBsPopperConfig,');
  FPack.Add('        ...execute(this._config.popperConfig, [defaultBsPopperConfig])');
  FPack.Add('      };');
  FPack.Add('    }');
  FPack.Add('    _setListeners() {');
  FPack.Add('      const triggers = this._config.trigger.split('' '');');
  FPack.Add('      for (const trigger of triggers) {');
  FPack.Add('        if (trigger === ''click'') {');
  FPack.Add('          EventHandler.on(this._element, this.constructor.eventName(EVENT_CLICK$1), this._config.selector, event => {');
  FPack.Add('            const context = this._initializeOnDelegatedTarget(event);');
  FPack.Add('            context.toggle();');
  FPack.Add('          });');
  FPack.Add('        } else if (trigger !== TRIGGER_MANUAL) {');
  FPack.Add('          const eventIn = trigger === TRIGGER_HOVER ? this.constructor.eventName(EVENT_MOUSEENTER) : this.constructor.eventName(EVENT_FOCUSIN$1);');
  FPack.Add('          const eventOut = trigger === TRIGGER_HOVER ? this.constructor.eventName(EVENT_MOUSELEAVE) : this.constructor.eventName(EVENT_FOCUSOUT$1);');
  FPack.Add('          EventHandler.on(this._element, eventIn, this._config.selector, event => {');
  FPack.Add('            const context = this._initializeOnDelegatedTarget(event);');
  FPack.Add('            context._activeTrigger[event.type === ''focusin'' ? TRIGGER_FOCUS : TRIGGER_HOVER] = true;');
  FPack.Add('            context._enter();');
  FPack.Add('          });');
  FPack.Add('          EventHandler.on(this._element, eventOut, this._config.selector, event => {');
  FPack.Add('            const context = this._initializeOnDelegatedTarget(event);');
  FPack.Add('            context._activeTrigger[event.type === ''focusout'' ? TRIGGER_FOCUS : TRIGGER_HOVER] = context._element.contains(event.relatedTarget);');
  FPack.Add('            context._leave();');
  FPack.Add('          });');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      this._hideModalHandler = () => {');
  FPack.Add('        if (this._element) {');
  FPack.Add('          this.hide();');
  FPack.Add('        }');
  FPack.Add('      };');
  FPack.Add('      EventHandler.on(this._element.closest(SELECTOR_MODAL), EVENT_MODAL_HIDE, this._hideModalHandler);');
  FPack.Add('    }');
  FPack.Add('    _fixTitle() {');
  FPack.Add('      const title = this._element.getAttribute(''title'');');
  FPack.Add('      if (!title) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      if (!this._element.getAttribute(''aria-label'') && !this._element.textContent.trim()) {');
  FPack.Add('        this._element.setAttribute(''aria-label'', title);');
  FPack.Add('      }');
  FPack.Add('      this._element.setAttribute(''data-bs-original-title'', title); // DO NOT USE IT. Is only for backwards compatibility');
  FPack.Add('      this._element.removeAttribute(''title'');');
  FPack.Add('    }');
  FPack.Add('    _enter() {');
  FPack.Add('      if (this._isShown() || this._isHovered) {');
  FPack.Add('        this._isHovered = true;');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isHovered = true;');
  FPack.Add('      this._setTimeout(() => {');
  FPack.Add('        if (this._isHovered) {');
  FPack.Add('          this.show();');
  FPack.Add('        }');
  FPack.Add('      }, this._config.delay.show);');
  FPack.Add('    }');
  FPack.Add('    _leave() {');
  FPack.Add('      if (this._isWithActiveTrigger()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._isHovered = false;');
  FPack.Add('      this._setTimeout(() => {');
  FPack.Add('        if (!this._isHovered) {');
  FPack.Add('          this.hide();');
  FPack.Add('        }');
  FPack.Add('      }, this._config.delay.hide);');
  FPack.Add('    }');
  FPack.Add('    _setTimeout(handler, timeout) {');
  FPack.Add('      clearTimeout(this._timeout);');
  FPack.Add('      this._timeout = setTimeout(handler, timeout);');
  FPack.Add('    }');
  FPack.Add('    _isWithActiveTrigger() {');
  FPack.Add('      return Object.values(this._activeTrigger).includes(true);');
  FPack.Add('    }');
  FPack.Add('    _getConfig(config) {');
  FPack.Add('      const dataAttributes = Manipulator.getDataAttributes(this._element);');
  FPack.Add('      for (const dataAttribute of Object.keys(dataAttributes)) {');
  FPack.Add('        if (DISALLOWED_ATTRIBUTES.has(dataAttribute)) {');
  FPack.Add('          delete dataAttributes[dataAttribute];');
end;

procedure TBootstrapJS.BootstrapJS_10;
begin
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      config = {');
  FPack.Add('        ...dataAttributes,');
  FPack.Add('        ...(typeof config === ''object'' && config ? config : {})');
  FPack.Add('      };');
  FPack.Add('      config = this._mergeConfigObj(config);');
  FPack.Add('      config = this._configAfterMerge(config);');
  FPack.Add('      this._typeCheckConfig(config);');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      config.container = config.container === false ? document.body : getElement(config.container);');
  FPack.Add('      if (typeof config.delay === ''number'') {');
  FPack.Add('        config.delay = {');
  FPack.Add('          show: config.delay,');
  FPack.Add('          hide: config.delay');
  FPack.Add('        };');
  FPack.Add('      }');
  FPack.Add('      if (typeof config.title === ''number'') {');
  FPack.Add('        config.title = config.title.toString();');
  FPack.Add('      }');
  FPack.Add('      if (typeof config.content === ''number'') {');
  FPack.Add('        config.content = config.content.toString();');
  FPack.Add('      }');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _getDelegateConfig() {');
  FPack.Add('      const config = {};');
  FPack.Add('      for (const [key, value] of Object.entries(this._config)) {');
  FPack.Add('        if (this.constructor.Default[key] !== value) {');
  FPack.Add('          config[key] = value;');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('      config.selector = false;');
  FPack.Add('      config.trigger = ''manual'';');
  FPack.Add('');
  FPack.Add('      // In the future can be replaced with:');
  FPack.Add('      // const keysWithDifferentValues = Object.entries(this._config).filter(entry => this.constructor.Default[entry[0]] !== this._config[entry[0]])');
  FPack.Add('      // `Object.fromEntries(keysWithDifferentValues)`');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _disposePopper() {');
  FPack.Add('      if (this._popper) {');
  FPack.Add('        this._popper.destroy();');
  FPack.Add('        this._popper = null;');
  FPack.Add('      }');
  FPack.Add('      if (this.tip) {');
  FPack.Add('        this.tip.remove();');
  FPack.Add('        this.tip = null;');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Tooltip.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (typeof data[config] === ''undefined'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config]();');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Tooltip);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap popover.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$3 = ''popover'';');
  FPack.Add('  const SELECTOR_TITLE = ''.popover-header'';');
  FPack.Add('  const SELECTOR_CONTENT = ''.popover-body'';');
  FPack.Add('  const Default$2 = {');
  FPack.Add('    ...Tooltip.Default,');
  FPack.Add('    content: '''',');
  FPack.Add('    offset: [0, 8],');
  FPack.Add('    placement: ''right'',');
  FPack.Add('    template: ''<div class="popover" role="tooltip">'' + ''<div class="popover-arrow"></div>'' + ''<h3 class="popover-header"></h3>'' + ''<div class="popover-body"></div>'' + ''</div>'',');
  FPack.Add('    trigger: ''click''');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$2 = {');
  FPack.Add('    ...Tooltip.DefaultType,');
  FPack.Add('    content: ''(null|string|element|function)''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Popover extends Tooltip {');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$2;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$2;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$3;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Overrides');
  FPack.Add('    _isWithContent() {');
  FPack.Add('      return this._getTitle() || this._getContent();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _getContentForTemplate() {');
  FPack.Add('      return {');
  FPack.Add('        [SELECTOR_TITLE]: this._getTitle(),');
  FPack.Add('        [SELECTOR_CONTENT]: this._getContent()');
  FPack.Add('      };');
  FPack.Add('    }');
  FPack.Add('    _getContent() {');
  FPack.Add('      return this._resolvePossibleFunction(this._config.content);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Popover.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (typeof data[config] === ''undefined'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config]();');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Popover);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap scrollspy.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$2 = ''scrollspy'';');
  FPack.Add('  const DATA_KEY$2 = ''bs.scrollspy'';');
  FPack.Add('  const EVENT_KEY$2 = `.${DATA_KEY$2}`;');
  FPack.Add('  const DATA_API_KEY = ''.data-api'';');
  FPack.Add('  const EVENT_ACTIVATE = `activate${EVENT_KEY$2}`;');
  FPack.Add('  const EVENT_CLICK = `click${EVENT_KEY$2}`;');
  FPack.Add('  const EVENT_LOAD_DATA_API$1 = `load${EVENT_KEY$2}${DATA_API_KEY}`;');
  FPack.Add('  const CLASS_NAME_DROPDOWN_ITEM = ''dropdown-item'';');
  FPack.Add('  const CLASS_NAME_ACTIVE$1 = ''active'';');
  FPack.Add('  const SELECTOR_DATA_SPY = ''[data-bs-spy="scroll"]'';');
  FPack.Add('  const SELECTOR_TARGET_LINKS = ''[href]'';');
  FPack.Add('  const SELECTOR_NAV_LIST_GROUP = ''.nav, .list-group'';');
  FPack.Add('  const SELECTOR_NAV_LINKS = ''.nav-link'';');
  FPack.Add('  const SELECTOR_NAV_ITEMS = ''.nav-item'';');
  FPack.Add('  const SELECTOR_LIST_ITEMS = ''.list-group-item'';');
  FPack.Add('  const SELECTOR_LINK_ITEMS = `${SELECTOR_NAV_LINKS}, ${SELECTOR_NAV_ITEMS} > ${SELECTOR_NAV_LINKS}, ${SELECTOR_LIST_ITEMS}`;');
  FPack.Add('  const SELECTOR_DROPDOWN = ''.dropdown'';');
  FPack.Add('  const SELECTOR_DROPDOWN_TOGGLE$1 = ''.dropdown-toggle'';');
  FPack.Add('  const Default$1 = {');
  FPack.Add('    offset: null,');
  FPack.Add('    // TODO: v6 @deprecated, keep it for backwards compatibility reasons');
  FPack.Add('    rootMargin: ''0px 0px -25%'',');
  FPack.Add('    smoothScroll: false,');
  FPack.Add('    target: null,');
  FPack.Add('    threshold: [0.1, 0.5, 1]');
  FPack.Add('  };');
  FPack.Add('  const DefaultType$1 = {');
  FPack.Add('    offset: ''(number|null)'',');
  FPack.Add('    // TODO v6 @deprecated, keep it for backwards compatibility reasons');
  FPack.Add('    rootMargin: ''string'',');
  FPack.Add('    smoothScroll: ''boolean'',');
  FPack.Add('    target: ''element'',');
  FPack.Add('    threshold: ''array''');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class ScrollSpy extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('');
  FPack.Add('      // this._element is the observablesContainer and config.target the menu links wrapper');
  FPack.Add('      this._targetLinks = new Map();');
  FPack.Add('      this._observableSections = new Map();');
  FPack.Add('      this._rootElement = getComputedStyle(this._element).overflowY === ''visible'' ? null : this._element;');
  FPack.Add('      this._activeTarget = null;');
  FPack.Add('      this._observer = null;');
  FPack.Add('      this._previousScrollData = {');
  FPack.Add('        visibleEntryTop: 0,');
  FPack.Add('        parentScrollTop: 0');
  FPack.Add('      };');
  FPack.Add('      this.refresh(); // initialize');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default$1;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType$1;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$2;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    refresh() {');
  FPack.Add('      this._initializeTargetsAndObservables();');
  FPack.Add('      this._maybeEnableSmoothScroll();');
  FPack.Add('      if (this._observer) {');
  FPack.Add('        this._observer.disconnect();');
  FPack.Add('      } else {');
  FPack.Add('        this._observer = this._getNewObserver();');
  FPack.Add('      }');
  FPack.Add('      for (const section of this._observableSections.values()) {');
  FPack.Add('        this._observer.observe(section);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      this._observer.disconnect();');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _configAfterMerge(config) {');
  FPack.Add('      // TODO: on v6 target should be given explicitly & remove the {target: ''ss-target''} case');
  FPack.Add('      config.target = getElement(config.target) || document.body;');
  FPack.Add('');
  FPack.Add('      // TODO: v6 Only for backwards compatibility reasons. Use rootMargin only');
  FPack.Add('      config.rootMargin = config.offset ? `${config.offset}px 0px -30%` : config.rootMargin;');
  FPack.Add('      if (typeof config.threshold === ''string'') {');
  FPack.Add('        config.threshold = config.threshold.split('','').map(value => Number.parseFloat(value));');
  FPack.Add('      }');
  FPack.Add('      return config;');
  FPack.Add('    }');
  FPack.Add('    _maybeEnableSmoothScroll() {');
  FPack.Add('      if (!this._config.smoothScroll) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // unregister any previous listeners');
  FPack.Add('      EventHandler.off(this._config.target, EVENT_CLICK);');
  FPack.Add('      EventHandler.on(this._config.target, EVENT_CLICK, SELECTOR_TARGET_LINKS, event => {');
  FPack.Add('        const observableSection = this._observableSections.get(event.target.hash);');
  FPack.Add('        if (observableSection) {');
  FPack.Add('          event.preventDefault();');
  FPack.Add('          const root = this._rootElement || window;');
  FPack.Add('          const height = observableSection.offsetTop - this._element.offsetTop;');
  FPack.Add('          if (root.scrollTo) {');
  FPack.Add('            root.scrollTo({');
  FPack.Add('              top: height,');
  FPack.Add('              behavior: ''smooth''');
  FPack.Add('            });');
  FPack.Add('            return;');
  FPack.Add('          }');
  FPack.Add('');
  FPack.Add('          // Chrome 60 doesn''t support `scrollTo`');
  FPack.Add('          root.scrollTop = height;');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _getNewObserver() {');
  FPack.Add('      const options = {');
  FPack.Add('        root: this._rootElement,');
  FPack.Add('        threshold: this._config.threshold,');
  FPack.Add('        rootMargin: this._config.rootMargin');
  FPack.Add('      };');
  FPack.Add('      return new IntersectionObserver(entries => this._observerCallback(entries), options);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // The logic of selection');
  FPack.Add('    _observerCallback(entries) {');
  FPack.Add('      const targetElement = entry => this._targetLinks.get(`#${entry.target.id}`);');
  FPack.Add('      const activate = entry => {');
  FPack.Add('        this._previousScrollData.visibleEntryTop = entry.target.offsetTop;');
  FPack.Add('        this._process(targetElement(entry));');
  FPack.Add('      };');
  FPack.Add('      const parentScrollTop = (this._rootElement || document.documentElement).scrollTop;');
  FPack.Add('      const userScrollsDown = parentScrollTop >= this._previousScrollData.parentScrollTop;');
  FPack.Add('      this._previousScrollData.parentScrollTop = parentScrollTop;');
  FPack.Add('      for (const entry of entries) {');
  FPack.Add('        if (!entry.isIntersecting) {');
  FPack.Add('          this._activeTarget = null;');
  FPack.Add('          this._clearActiveClass(targetElement(entry));');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('        const entryIsLowerThanPrevious = entry.target.offsetTop >= this._previousScrollData.visibleEntryTop;');
  FPack.Add('        // if we are scrolling down, pick the bigger offsetTop');
  FPack.Add('        if (userScrollsDown && entryIsLowerThanPrevious) {');
  FPack.Add('          activate(entry);');
  FPack.Add('          // if parent isn''t scrolled, let''s keep the first visible item, breaking the iteration');
  FPack.Add('          if (!parentScrollTop) {');
  FPack.Add('            return;');
  FPack.Add('          }');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('');
  FPack.Add('        // if we are scrolling up, pick the smallest offsetTop');
  FPack.Add('        if (!userScrollsDown && !entryIsLowerThanPrevious) {');
  FPack.Add('          activate(entry);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _initializeTargetsAndObservables() {');
  FPack.Add('      this._targetLinks = new Map();');
  FPack.Add('      this._observableSections = new Map();');
  FPack.Add('      const targetLinks = SelectorEngine.find(SELECTOR_TARGET_LINKS, this._config.target);');
  FPack.Add('      for (const anchor of targetLinks) {');
  FPack.Add('        // ensure that the anchor has an id and is not disabled');
  FPack.Add('        if (!anchor.hash || isDisabled(anchor)) {');
  FPack.Add('          continue;');
  FPack.Add('        }');
  FPack.Add('        const observableSection = SelectorEngine.findOne(decodeURI(anchor.hash), this._element);');
  FPack.Add('');
  FPack.Add('        // ensure that the observableSection exists & is visible');
  FPack.Add('        if (isVisible(observableSection)) {');
  FPack.Add('          this._targetLinks.set(decodeURI(anchor.hash), anchor);');
  FPack.Add('          this._observableSections.set(anchor.hash, observableSection);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _process(target) {');
  FPack.Add('      if (this._activeTarget === target) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._clearActiveClass(this._config.target);');
  FPack.Add('      this._activeTarget = target;');
  FPack.Add('      target.classList.add(CLASS_NAME_ACTIVE$1);');
  FPack.Add('      this._activateParents(target);');
  FPack.Add('      EventHandler.trigger(this._element, EVENT_ACTIVATE, {');
  FPack.Add('        relatedTarget: target');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('    _activateParents(target) {');
  FPack.Add('      // Activate dropdown parents');
  FPack.Add('      if (target.classList.contains(CLASS_NAME_DROPDOWN_ITEM)) {');
  FPack.Add('        SelectorEngine.findOne(SELECTOR_DROPDOWN_TOGGLE$1, target.closest(SELECTOR_DROPDOWN)).classList.add(CLASS_NAME_ACTIVE$1);');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      for (const listGroup of SelectorEngine.parents(target, SELECTOR_NAV_LIST_GROUP)) {');
  FPack.Add('        // Set triggered links parents as active');
  FPack.Add('        // With both <ul> and <nav> markup a parent is the previous sibling of any nav ancestor');
  FPack.Add('        for (const item of SelectorEngine.prev(listGroup, SELECTOR_LINK_ITEMS)) {');
  FPack.Add('          item.classList.add(CLASS_NAME_ACTIVE$1);');
  FPack.Add('        }');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _clearActiveClass(parent) {');
  FPack.Add('      parent.classList.remove(CLASS_NAME_ACTIVE$1);');
  FPack.Add('      const activeNodes = SelectorEngine.find(`${SELECTOR_TARGET_LINKS}.${CLASS_NAME_ACTIVE$1}`, parent);');
  FPack.Add('      for (const node of activeNodes) {');
  FPack.Add('        node.classList.remove(CLASS_NAME_ACTIVE$1);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = ScrollSpy.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (data[config] === undefined || config.startsWith(''_'') || config === ''constructor'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config]();');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
end;

procedure TBootstrapJS.BootstrapJS_11;
begin
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(window, EVENT_LOAD_DATA_API$1, () => {');
  FPack.Add('    for (const spy of SelectorEngine.find(SELECTOR_DATA_SPY)) {');
  FPack.Add('      ScrollSpy.getOrCreateInstance(spy);');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(ScrollSpy);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap tab.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME$1 = ''tab'';');
  FPack.Add('  const DATA_KEY$1 = ''bs.tab'';');
  FPack.Add('  const EVENT_KEY$1 = `.${DATA_KEY$1}`;');
  FPack.Add('  const EVENT_HIDE$1 = `hide${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_HIDDEN$1 = `hidden${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_SHOW$1 = `show${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_SHOWN$1 = `shown${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_CLICK_DATA_API = `click${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_KEYDOWN = `keydown${EVENT_KEY$1}`;');
  FPack.Add('  const EVENT_LOAD_DATA_API = `load${EVENT_KEY$1}`;');
  FPack.Add('  const ARROW_LEFT_KEY = ''ArrowLeft'';');
  FPack.Add('  const ARROW_RIGHT_KEY = ''ArrowRight'';');
  FPack.Add('  const ARROW_UP_KEY = ''ArrowUp'';');
  FPack.Add('  const ARROW_DOWN_KEY = ''ArrowDown'';');
  FPack.Add('  const HOME_KEY = ''Home'';');
  FPack.Add('  const END_KEY = ''End'';');
  FPack.Add('  const CLASS_NAME_ACTIVE = ''active'';');
  FPack.Add('  const CLASS_NAME_FADE$1 = ''fade'';');
  FPack.Add('  const CLASS_NAME_SHOW$1 = ''show'';');
  FPack.Add('  const CLASS_DROPDOWN = ''dropdown'';');
  FPack.Add('  const SELECTOR_DROPDOWN_TOGGLE = ''.dropdown-toggle'';');
  FPack.Add('  const SELECTOR_DROPDOWN_MENU = ''.dropdown-menu'';');
  FPack.Add('  const NOT_SELECTOR_DROPDOWN_TOGGLE = `:not(${SELECTOR_DROPDOWN_TOGGLE})`;');
  FPack.Add('  const SELECTOR_TAB_PANEL = ''.list-group, .nav, [role="tablist"]'';');
  FPack.Add('  const SELECTOR_OUTER = ''.nav-item, .list-group-item'';');
  FPack.Add('  const SELECTOR_INNER = `.nav-link${NOT_SELECTOR_DROPDOWN_TOGGLE}, .list-group-item${NOT_SELECTOR_DROPDOWN_TOGGLE}, [role="tab"]${NOT_SELECTOR_DROPDOWN_TOGGLE}`;');
  FPack.Add('  const SELECTOR_DATA_TOGGLE = ''[data-bs-toggle="tab"], [data-bs-toggle="pill"], [data-bs-toggle="list"]''; // TODO: could only be `tab` in v6');
  FPack.Add('  const SELECTOR_INNER_ELEM = `${SELECTOR_INNER}, ${SELECTOR_DATA_TOGGLE}`;');
  FPack.Add('  const SELECTOR_DATA_TOGGLE_ACTIVE = `.${CLASS_NAME_ACTIVE}[data-bs-toggle="tab"], .${CLASS_NAME_ACTIVE}[data-bs-toggle="pill"], .${CLASS_NAME_ACTIVE}[data-bs-toggle="list"]`;');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Tab extends BaseComponent {');
  FPack.Add('    constructor(element) {');
  FPack.Add('      super(element);');
  FPack.Add('      this._parent = this._element.closest(SELECTOR_TAB_PANEL);');
  FPack.Add('      if (!this._parent) {');
  FPack.Add('        return;');
  FPack.Add('        // TODO: should throw exception in v6');
  FPack.Add('        // throw new TypeError(`${element.outerHTML} has not a valid parent ${SELECTOR_INNER_ELEM}`)');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // Set up initial aria attributes');
  FPack.Add('      this._setInitialAttributes(this._parent, this._getChildren());');
  FPack.Add('      EventHandler.on(this._element, EVENT_KEYDOWN, event => this._keydown(event));');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME$1;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    show() {');
  FPack.Add('      // Shows this elem and deactivate the active sibling if exists');
  FPack.Add('      const innerElem = this._element;');
  FPack.Add('      if (this._elemIsActive(innerElem)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('');
  FPack.Add('      // Search for active tab on same parent to deactivate it');
  FPack.Add('      const active = this._getActiveElem();');
  FPack.Add('      const hideEvent = active ? EventHandler.trigger(active, EVENT_HIDE$1, {');
  FPack.Add('        relatedTarget: innerElem');
  FPack.Add('      }) : null;');
  FPack.Add('      const showEvent = EventHandler.trigger(innerElem, EVENT_SHOW$1, {');
  FPack.Add('        relatedTarget: active');
  FPack.Add('      });');
  FPack.Add('      if (showEvent.defaultPrevented || hideEvent && hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._deactivate(active, innerElem);');
  FPack.Add('      this._activate(innerElem, active);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('    _activate(element, relatedElem) {');
  FPack.Add('      if (!element) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      element.classList.add(CLASS_NAME_ACTIVE);');
  FPack.Add('      this._activate(SelectorEngine.getElementFromSelector(element)); // Search and activate/show the proper section');
  FPack.Add('');
  FPack.Add('      const complete = () => {');
  FPack.Add('        if (element.getAttribute(''role'') !== ''tab'') {');
  FPack.Add('          element.classList.add(CLASS_NAME_SHOW$1);');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        element.removeAttribute(''tabindex'');');
  FPack.Add('        element.setAttribute(''aria-selected'', true);');
  FPack.Add('        this._toggleDropDown(element, true);');
  FPack.Add('        EventHandler.trigger(element, EVENT_SHOWN$1, {');
  FPack.Add('          relatedTarget: relatedElem');
  FPack.Add('        });');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(complete, element, element.classList.contains(CLASS_NAME_FADE$1));');
  FPack.Add('    }');
  FPack.Add('    _deactivate(element, relatedElem) {');
  FPack.Add('      if (!element) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      element.classList.remove(CLASS_NAME_ACTIVE);');
  FPack.Add('      element.blur();');
  FPack.Add('      this._deactivate(SelectorEngine.getElementFromSelector(element)); // Search and deactivate the shown section too');
  FPack.Add('');
  FPack.Add('      const complete = () => {');
  FPack.Add('        if (element.getAttribute(''role'') !== ''tab'') {');
  FPack.Add('          element.classList.remove(CLASS_NAME_SHOW$1);');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        element.setAttribute(''aria-selected'', false);');
  FPack.Add('        element.setAttribute(''tabindex'', ''-1'');');
  FPack.Add('        this._toggleDropDown(element, false);');
  FPack.Add('        EventHandler.trigger(element, EVENT_HIDDEN$1, {');
  FPack.Add('          relatedTarget: relatedElem');
  FPack.Add('        });');
  FPack.Add('      };');
  FPack.Add('      this._queueCallback(complete, element, element.classList.contains(CLASS_NAME_FADE$1));');
  FPack.Add('    }');
  FPack.Add('    _keydown(event) {');
  FPack.Add('      if (![ARROW_LEFT_KEY, ARROW_RIGHT_KEY, ARROW_UP_KEY, ARROW_DOWN_KEY, HOME_KEY, END_KEY].includes(event.key)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      event.stopPropagation(); // stopPropagation/preventDefault both added to support up/down keys without scrolling the page');
  FPack.Add('      event.preventDefault();');
  FPack.Add('      const children = this._getChildren().filter(element => !isDisabled(element));');
  FPack.Add('      let nextActiveElement;');
  FPack.Add('      if ([HOME_KEY, END_KEY].includes(event.key)) {');
  FPack.Add('        nextActiveElement = children[event.key === HOME_KEY ? 0 : children.length - 1];');
  FPack.Add('      } else {');
  FPack.Add('        const isNext = [ARROW_RIGHT_KEY, ARROW_DOWN_KEY].includes(event.key);');
  FPack.Add('        nextActiveElement = getNextActiveElement(children, event.target, isNext, true);');
  FPack.Add('      }');
  FPack.Add('      if (nextActiveElement) {');
  FPack.Add('        nextActiveElement.focus({');
  FPack.Add('          preventScroll: true');
  FPack.Add('        });');
  FPack.Add('        Tab.getOrCreateInstance(nextActiveElement).show();');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _getChildren() {');
  FPack.Add('      // collection of inner elements');
  FPack.Add('      return SelectorEngine.find(SELECTOR_INNER_ELEM, this._parent);');
  FPack.Add('    }');
  FPack.Add('    _getActiveElem() {');
  FPack.Add('      return this._getChildren().find(child => this._elemIsActive(child)) || null;');
  FPack.Add('    }');
  FPack.Add('    _setInitialAttributes(parent, children) {');
  FPack.Add('      this._setAttributeIfNotExists(parent, ''role'', ''tablist'');');
  FPack.Add('      for (const child of children) {');
  FPack.Add('        this._setInitialAttributesOnChild(child);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _setInitialAttributesOnChild(child) {');
  FPack.Add('      child = this._getInnerElement(child);');
  FPack.Add('      const isActive = this._elemIsActive(child);');
  FPack.Add('      const outerElem = this._getOuterElement(child);');
  FPack.Add('      child.setAttribute(''aria-selected'', isActive);');
  FPack.Add('      if (outerElem !== child) {');
  FPack.Add('        this._setAttributeIfNotExists(outerElem, ''role'', ''presentation'');');
  FPack.Add('      }');
  FPack.Add('      if (!isActive) {');
  FPack.Add('        child.setAttribute(''tabindex'', ''-1'');');
  FPack.Add('      }');
  FPack.Add('      this._setAttributeIfNotExists(child, ''role'', ''tab'');');
  FPack.Add('');
  FPack.Add('      // set attributes to the related panel too');
  FPack.Add('      this._setInitialAttributesOnTargetPanel(child);');
  FPack.Add('    }');
  FPack.Add('    _setInitialAttributesOnTargetPanel(child) {');
  FPack.Add('      const target = SelectorEngine.getElementFromSelector(child);');
  FPack.Add('      if (!target) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._setAttributeIfNotExists(target, ''role'', ''tabpanel'');');
  FPack.Add('      if (child.id) {');
  FPack.Add('        this._setAttributeIfNotExists(target, ''aria-labelledby'', `${child.id}`);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _toggleDropDown(element, open) {');
  FPack.Add('      const outerElem = this._getOuterElement(element);');
  FPack.Add('      if (!outerElem.classList.contains(CLASS_DROPDOWN)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const toggle = (selector, className) => {');
  FPack.Add('        const element = SelectorEngine.findOne(selector, outerElem);');
  FPack.Add('        if (element) {');
  FPack.Add('          element.classList.toggle(className, open);');
  FPack.Add('        }');
  FPack.Add('      };');
  FPack.Add('      toggle(SELECTOR_DROPDOWN_TOGGLE, CLASS_NAME_ACTIVE);');
  FPack.Add('      toggle(SELECTOR_DROPDOWN_MENU, CLASS_NAME_SHOW$1);');
  FPack.Add('      outerElem.setAttribute(''aria-expanded'', open);');
  FPack.Add('    }');
  FPack.Add('    _setAttributeIfNotExists(element, attribute, value) {');
  FPack.Add('      if (!element.hasAttribute(attribute)) {');
  FPack.Add('        element.setAttribute(attribute, value);');
  FPack.Add('      }');
  FPack.Add('    }');
  FPack.Add('    _elemIsActive(elem) {');
  FPack.Add('      return elem.classList.contains(CLASS_NAME_ACTIVE);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Try to get the inner element (usually the .nav-link)');
  FPack.Add('    _getInnerElement(elem) {');
  FPack.Add('      return elem.matches(SELECTOR_INNER_ELEM) ? elem : SelectorEngine.findOne(SELECTOR_INNER_ELEM, elem);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Try to get the outer element (usually the .nav-item)');
  FPack.Add('    _getOuterElement(elem) {');
  FPack.Add('      return elem.closest(SELECTOR_OUTER) || elem;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Tab.getOrCreateInstance(this);');
  FPack.Add('        if (typeof config !== ''string'') {');
  FPack.Add('          return;');
  FPack.Add('        }');
  FPack.Add('        if (data[config] === undefined || config.startsWith(''_'') || config === ''constructor'') {');
  FPack.Add('          throw new TypeError(`No method named "${config}"`);');
  FPack.Add('        }');
  FPack.Add('        data[config]();');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  EventHandler.on(document, EVENT_CLICK_DATA_API, SELECTOR_DATA_TOGGLE, function (event) {');
  FPack.Add('    if ([''A'', ''AREA''].includes(this.tagName)) {');
  FPack.Add('      event.preventDefault();');
  FPack.Add('    }');
  FPack.Add('    if (isDisabled(this)) {');
  FPack.Add('      return;');
  FPack.Add('    }');
  FPack.Add('    Tab.getOrCreateInstance(this).show();');
  FPack.Add('  });');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Initialize on focus');
  FPack.Add('   */');
  FPack.Add('  EventHandler.on(window, EVENT_LOAD_DATA_API, () => {');
  FPack.Add('    for (const element of SelectorEngine.find(SELECTOR_DATA_TOGGLE_ACTIVE)) {');
  FPack.Add('      Tab.getOrCreateInstance(element);');
  FPack.Add('    }');
  FPack.Add('  });');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Tab);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap toast.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Constants');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const NAME = ''toast'';');
  FPack.Add('  const DATA_KEY = ''bs.toast'';');
  FPack.Add('  const EVENT_KEY = `.${DATA_KEY}`;');
  FPack.Add('  const EVENT_MOUSEOVER = `mouseover${EVENT_KEY}`;');
  FPack.Add('  const EVENT_MOUSEOUT = `mouseout${EVENT_KEY}`;');
  FPack.Add('  const EVENT_FOCUSIN = `focusin${EVENT_KEY}`;');
  FPack.Add('  const EVENT_FOCUSOUT = `focusout${EVENT_KEY}`;');
  FPack.Add('  const EVENT_HIDE = `hide${EVENT_KEY}`;');
  FPack.Add('  const EVENT_HIDDEN = `hidden${EVENT_KEY}`;');
  FPack.Add('  const EVENT_SHOW = `show${EVENT_KEY}`;');
  FPack.Add('  const EVENT_SHOWN = `shown${EVENT_KEY}`;');
  FPack.Add('  const CLASS_NAME_FADE = ''fade'';');
  FPack.Add('  const CLASS_NAME_HIDE = ''hide''; // @deprecated - kept here only for backwards compatibility');
  FPack.Add('  const CLASS_NAME_SHOW = ''show'';');
  FPack.Add('  const CLASS_NAME_SHOWING = ''showing'';');
  FPack.Add('  const DefaultType = {');
  FPack.Add('    animation: ''boolean'',');
  FPack.Add('    autohide: ''boolean'',');
  FPack.Add('    delay: ''number''');
  FPack.Add('  };');
  FPack.Add('  const Default = {');
  FPack.Add('    animation: true,');
  FPack.Add('    autohide: true,');
  FPack.Add('    delay: 5000');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Class definition');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  class Toast extends BaseComponent {');
  FPack.Add('    constructor(element, config) {');
  FPack.Add('      super(element, config);');
  FPack.Add('      this._timeout = null;');
  FPack.Add('      this._hasMouseInteraction = false;');
  FPack.Add('      this._hasKeyboardInteraction = false;');
  FPack.Add('      this._setListeners();');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Getters');
  FPack.Add('    static get Default() {');
  FPack.Add('      return Default;');
  FPack.Add('    }');
  FPack.Add('    static get DefaultType() {');
  FPack.Add('      return DefaultType;');
  FPack.Add('    }');
  FPack.Add('    static get NAME() {');
  FPack.Add('      return NAME;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Public');
  FPack.Add('    show() {');
  FPack.Add('      const showEvent = EventHandler.trigger(this._element, EVENT_SHOW);');
  FPack.Add('      if (showEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._clearTimeout();');
  FPack.Add('      if (this._config.animation) {');
  FPack.Add('        this._element.classList.add(CLASS_NAME_FADE);');
  FPack.Add('      }');
  FPack.Add('      const complete = () => {');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_SHOWING);');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_SHOWN);');
  FPack.Add('        this._maybeScheduleHide();');
  FPack.Add('      };');
  FPack.Add('      this._element.classList.remove(CLASS_NAME_HIDE); // @deprecated');
  FPack.Add('      reflow(this._element);');
  FPack.Add('      this._element.classList.add(CLASS_NAME_SHOW, CLASS_NAME_SHOWING);');
  FPack.Add('      this._queueCallback(complete, this._element, this._config.animation);');
  FPack.Add('    }');
  FPack.Add('    hide() {');
  FPack.Add('      if (!this.isShown()) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE);');
  FPack.Add('      if (hideEvent.defaultPrevented) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const complete = () => {');
  FPack.Add('        this._element.classList.add(CLASS_NAME_HIDE); // @deprecated');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_SHOWING, CLASS_NAME_SHOW);');
  FPack.Add('        EventHandler.trigger(this._element, EVENT_HIDDEN);');
  FPack.Add('      };');
  FPack.Add('      this._element.classList.add(CLASS_NAME_SHOWING);');
  FPack.Add('      this._queueCallback(complete, this._element, this._config.animation);');
  FPack.Add('    }');
  FPack.Add('    dispose() {');
  FPack.Add('      this._clearTimeout();');
  FPack.Add('      if (this.isShown()) {');
  FPack.Add('        this._element.classList.remove(CLASS_NAME_SHOW);');
  FPack.Add('      }');
  FPack.Add('      super.dispose();');
  FPack.Add('    }');
  FPack.Add('    isShown() {');
  FPack.Add('      return this._element.classList.contains(CLASS_NAME_SHOW);');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Private');
  FPack.Add('');
  FPack.Add('    _maybeScheduleHide() {');
  FPack.Add('      if (!this._config.autohide) {');
  FPack.Add('        return;');
  FPack.Add('      }');
end;

procedure TBootstrapJS.BootstrapJS_12;
begin
  FPack.Add('      if (this._hasMouseInteraction || this._hasKeyboardInteraction) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._timeout = setTimeout(() => {');
  FPack.Add('        this.hide();');
  FPack.Add('      }, this._config.delay);');
  FPack.Add('    }');
  FPack.Add('    _onInteraction(event, isInteracting) {');
  FPack.Add('      switch (event.type) {');
  FPack.Add('        case ''mouseover'':');
  FPack.Add('        case ''mouseout'':');
  FPack.Add('          {');
  FPack.Add('            this._hasMouseInteraction = isInteracting;');
  FPack.Add('            break;');
  FPack.Add('          }');
  FPack.Add('        case ''focusin'':');
  FPack.Add('        case ''focusout'':');
  FPack.Add('          {');
  FPack.Add('            this._hasKeyboardInteraction = isInteracting;');
  FPack.Add('            break;');
  FPack.Add('          }');
  FPack.Add('      }');
  FPack.Add('      if (isInteracting) {');
  FPack.Add('        this._clearTimeout();');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      const nextElement = event.relatedTarget;');
  FPack.Add('      if (this._element === nextElement || this._element.contains(nextElement)) {');
  FPack.Add('        return;');
  FPack.Add('      }');
  FPack.Add('      this._maybeScheduleHide();');
  FPack.Add('    }');
  FPack.Add('    _setListeners() {');
  FPack.Add('      EventHandler.on(this._element, EVENT_MOUSEOVER, event => this._onInteraction(event, true));');
  FPack.Add('      EventHandler.on(this._element, EVENT_MOUSEOUT, event => this._onInteraction(event, false));');
  FPack.Add('      EventHandler.on(this._element, EVENT_FOCUSIN, event => this._onInteraction(event, true));');
  FPack.Add('      EventHandler.on(this._element, EVENT_FOCUSOUT, event => this._onInteraction(event, false));');
  FPack.Add('    }');
  FPack.Add('    _clearTimeout() {');
  FPack.Add('      clearTimeout(this._timeout);');
  FPack.Add('      this._timeout = null;');
  FPack.Add('    }');
  FPack.Add('');
  FPack.Add('    // Static');
  FPack.Add('    static jQueryInterface(config) {');
  FPack.Add('      return this.each(function () {');
  FPack.Add('        const data = Toast.getOrCreateInstance(this, config);');
  FPack.Add('        if (typeof config === ''string'') {');
  FPack.Add('          if (typeof data[config] === ''undefined'') {');
  FPack.Add('            throw new TypeError(`No method named "${config}"`);');
  FPack.Add('          }');
  FPack.Add('          data[config](this);');
  FPack.Add('        }');
  FPack.Add('      });');
  FPack.Add('    }');
  FPack.Add('  }');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * Data API implementation');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  enableDismissTrigger(Toast);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * jQuery');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  defineJQueryPlugin(Toast);');
  FPack.Add('');
  FPack.Add('  /**');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   * Bootstrap index.umd.js');
  FPack.Add('   * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)');
  FPack.Add('   * --------------------------------------------------------------------------');
  FPack.Add('   */');
  FPack.Add('');
  FPack.Add('  const index_umd = {');
  FPack.Add('    Alert,');
  FPack.Add('    Button,');
  FPack.Add('    Carousel,');
  FPack.Add('    Collapse,');
  FPack.Add('    Dropdown,');
  FPack.Add('    Modal,');
  FPack.Add('    Offcanvas,');
  FPack.Add('    Popover,');
  FPack.Add('    ScrollSpy,');
  FPack.Add('    Tab,');
  FPack.Add('    Toast,');
  FPack.Add('    Tooltip');
  FPack.Add('  };');
  FPack.Add('');
  FPack.Add('  return index_umd;');
  FPack.Add('');
  FPack.Add('}));');
  FPack.Add('//# sourceMappingURL=bootstrap.js.map');
  FPack.Add('');
  FPack.Add('</script>'+#13);
end;
function TBootstrapJS.PackJS : String;
begin
  if FCDN then
    Result := '<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>'
  else
  begin
    BootstrapJS_1;
    BootstrapJS_2;
    BootstrapJS_3;
    BootstrapJS_4;
    BootstrapJS_5;
    BootstrapJS_6;
    BootstrapJS_7;
    BootstrapJS_8;
    BootstrapJS_9;
    BootstrapJS_10;
    BootstrapJS_11;
    BootstrapJS_12;
    Result := FPack.Text;
  end;
end;

end.
